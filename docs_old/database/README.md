# 数据库实施导航

数据库实施以 [数据库设计总纲](../数据库设计总纲.md) 为候选基线，以 `database/postgresql` 下的 SQL 为可执行权威来源；算法与密码合规开放项关闭并完成真实 PostgreSQL 验证后，方可标记为最终冻结基线。

本目录只描述业务模型是否能被数据库事实完整表达、哪些关系需要持久化以及 PostgreSQL 应承担哪些硬边界。代码目录、类、接口、Handler、Repository、API DTO 和外部组件调用顺序不在本目录定义；命令、事务、权限和校验边界由 [`docs/代码实施`](../代码实施/代码实施索引.md) 展开。

## 交付物

| 位置 | 用途 |
|---|---|
| `database/postgresql/bootstrap` | 数据库角色和数据库级最小权限 |
| `database/postgresql/migrations` | 113 张表、约束、索引、分区和运行时权限 |
| `database/postgresql/run-migrations.sh` | 按版本执行 Migration，并在独立技术 Schema 中登记脚本名、SHA-256、执行时间和登录身份 |
| `database/postgresql/seeds` | 稳定目录和 DRAFT 控制面候选版本，不创建用户、租户、Client 或秘密 |
| `database/postgresql/verification` | Schema、Comment、禁止对象、敏感字段、全部可解析逻辑关系、Seed 和权限门禁 |
| `业务模型与持久化边界清单.md` | 业务模型、持久化事实、数据库硬边界和非数据库职责索引 |
| `逻辑关系与非数据库校验清单.md` | 无 Foreign Key 时的逻辑引用登记和非数据库校验提示 |
| `需求编号与数据库持久化覆盖索引.md` | 能力地图/蓝图编号到数据库持久化边界的覆盖索引；不是代码实施或验收矩阵 |
| `查询与索引契约.md` | 安全关键和高频查询的过滤、索引及代码守卫契约 |
| `../代码实施/*.md` | 参数、逻辑引用、业务权限、事务、状态机、幂等和错误映射的代码实施契约 |
| `业务模型与持久化边界清单.md` §7 | 21 个能力域的持久化范围、权威边界和明确非数据库职责 |
| `generated/*.md` | 从 SQL 或数据库目录生成的只读报告 |

## 设计边界

- 数据库：存储事实、PK/Unique、基础 Check、技术默认时间、CAS 字段、索引、分区，并对 Migration、普通运行时、敏感数据、审计和 Worker 等部署身份做粗粒度技术隔离。
- 非数据库职责：引用存在性、租户一致性、状态转换、最终用户/管理员授权、数据范围、领域 Owner、风险、审批、加密、协议、幂等语义、流程和删除编排；具体契约见 [`docs/代码实施`](../代码实施/代码实施索引.md)。
- 数据权威：每张逻辑表映射到一个主要业务模型和权威域；多域复用按 `业务模型与持久化边界清单.md` 的共享表权威执行，不得形成未声明双写或影子状态。
- 数据库角色不映射最终用户、管理员角色、租户、数据范围或领域 Owner；这些业务边界由用户中心代码默认拒绝地校验。
- 审计、敏感存储和选定的高价值追加证据表可以使用表级/列级 Grant 做粗粒度防御纵深；它不证明业务授权、只追加命令或不可变版本规则正确。
- 所有运行时技术角色均无 `DELETE`；状态终止、匿名化、保留期和物理清理由代码与受审批的迁移/维护流程编排，数据库不使用级联或 Trigger 自动删除。
- 基线不依赖 RLS、数据库 Policy、`SECURITY DEFINER`、按租户建账号/Schema 或存储过程承载业务授权，避免迁移和升级被特定数据库能力锁定；若用于防御纵深，必须保留代码契约和可迁移降级路径。
- 当前候选 SQL 不创建 Foreign Key、业务 Trigger、持久化业务 Routine、PostgreSQL Enum 和业务 View。无 Foreign Key 策略在最终冻结前必须按 [ADR-0001](../架构决策/架构决策记录.md#adr-0001) 完成逐关系分类；这不改变当前 SQL 的可执行权威地位。
- 所有重要写入必须将权威数据、Outbox、审计事件和幂等结果放在同一应用事务中。

## 分区实现说明

总纲要求高容量表按月分区，同时要求 Outbox `event_id` 和 Inbox `(consumer_id,event_id)` 在数据库全局唯一。PostgreSQL Range 分区的唯一约束必须包含分区键，无法同时满足这两项。因此实施采用：

- `outbox_events`：按 `event_id` Hash 分区，数据库维持 `event_id` 全局唯一。
- `inbox_messages`：按 `(consumer_id,event_id)` Hash 分区，数据库维持消费幂等唯一。
- 其余 11 张高容量表：按时间月度 Range 分区。

该修正只改变物理分区方式，不改变业务契约。Outbox/Inbox 的时间保留通过 `recorded_at/received_at` 索引和运维归档完成。

## 版本支持

- PostgreSQL 15 及以上，使用 `UNIQUE NULLS NOT DISTINCT`。
- UUID 由应用生成，推荐 UUIDv7。
- Seed 使用 PostgreSQL 内置 `sha256(bytea)` 计算内容摘要，不依赖扩展；同键内容漂移直接失败，控制面对象默认 DRAFT。

## 发布门禁

1. 空数据库使用 `run-migrations.sh` 执行全部 Migration 并核对 `iam_meta.schema_migrations` 的 SHA-256，再按文件名顺序执行 Seed；已有无账本环境必须经单独评审后基线登记，禁止自动认领。
2. 执行全部 Verification，任一异常阻断发布。
3. 执行生成脚本，确认数据库需求覆盖编号总数与脚本输出一致（编号仅来源于能力地图和蓝图，不写死具体数值）、113/113 业务模型与领域持久化范围映射、全部多领域复用表权威、完整逻辑关系和数据库报告全部更新。
4. 按 [`docs/代码实施`](../代码实施/代码实施索引.md) 执行参数/Schema、状态机、逻辑引用、业务授权/数据范围、领域 Owner、租户隔离、追加/不可变历史篡改和敏感日志测试。
5. 检查未来 12 个月以上分区、默认分区积压、孤儿关系、Outbox 积压和安全水位传播。
6. 保存 Schema 快照、验证输出和迁移校验和作为发布证据。

本地环境跳过：本地没有可用 PostgreSQL 环境（无 `psql`、无容器运行时或无法启动）时，本地实跑与手工验证直接跳过，不阻断工程与文档建设；但上述发布门禁本身不豁免，必须在有条件的环境（托管数据库、CI 或受控测试库）补齐后再发布。规则全文见 [阶段 0 实施状态 §9 本地 PostgreSQL 跳过规则](../代码实施/阶段0实施状态.md#local-db-skip)。
