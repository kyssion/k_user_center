# 业务模型与数据库持久化边界清单

> 本文件只定义业务模型需要持久化的事实、逻辑关系和数据库应承担的硬边界，不定义具体语言、类、接口、Handler、Repository、API 路由或事务代码。代码实施契约位于 `docs/代码实施`，不得反向改变本文件已经确认的业务语义和持久化事实。

## 1. 数据库文档范围

数据库文档回答以下问题：

1. 业务模型是否有足够的稳定事实支撑状态恢复、查询、审计、撤销、对账和迁移。
2. 哪些对象拥有独立生命周期，哪些只是值对象、快照、事件或逻辑关系。
3. 哪些事实必须结构化为独立字段，哪些扩展内容可以进入版本化 JSON。
4. 哪些完整性边界必须由 PostgreSQL 最终兜底。
5. 哪些规则明确不属于数据库，后续实施必须在应用层完成。

数据库文档不回答代码目录、类名、方法签名、框架用法、外部组件调用顺序和 API DTO；命令级边界由 `docs/代码实施` 定义，具体代码结构由实现阶段决定。

## 2. 持久化原则

1. 只有独立、稳定、需要查询、审计、恢复或对账的数据事实才建表或独立字段，不把代码流程步骤机械翻译成表。
2. PostgreSQL 保存状态值、版本、原因、业务时间和结果事实，但不定义状态全集、合法转换、授权条件、风险处置或审批流程。
3. 当前候选 SQL 不创建 Foreign Key、业务 Trigger、持久化业务 Routine、PostgreSQL Enum 或表达业务语义的 View；Foreign Key 的最终策略须按 [ADR-0001](../架构决策/架构决策记录.md#adr-0001) 完成逐关系分类后冻结。
4. 逻辑引用只保存标识和查询索引；目标存在性、类型、租户/业务线一致性、生命周期和权限由后续实施负责。
5. Primary Key、Unique、NOT NULL、数据类型、长度、行内基础 Check、`row_version`、必要原子消费和面向部署身份的粗粒度技术权限属于数据库硬边界。
6. 唯一性和 CAS 不得只依赖“先查询再写入”的应用判断，数据库必须作为并发竞争的最终裁决者。
7. JSONB 只承载版本化配置、协议扩展、快照、证据和低频扩展属性；高频过滤、唯一性、逻辑关系、安全水位和生命周期字段必须独立建模。
8. `operations + operation_steps` 只保存跨事务流程的权威状态、检查点、结果和证据，不在数据库中建设工作流引擎。
9. Outbox、Audit Outbox、幂等结果与权威事实需要同事务持久化，但事件生成、投递、补偿和重试语义不在数据库中实现。
10. 敏感原文不落库；数据库只保存密文、摘要、盲索引、公开材料和 KMS/HSM 引用。
11. 追加事实和不可变版本以代码中的只追加命令/仓储接口、版本新增规则、摘要校验和审计为主要保护；审计、敏感存储和选定的高价值追加证据表可额外使用粗粒度数据库 Grant 防御纵深，但不要求数据库角色表达业务主体、作用域、Owner 或每个状态的可写性。
12. 数据库对象的存在不代表当前阶段必须实现全部业务能力；建设阶段由能力与实施计划决定，数据库模型保持目标态语义一致。
13. 每张逻辑表必须映射到一个主要业务模型和一个权威持久化域；领域文档中的“持久化范围”可以包含复用表，但不得据此形成第二个权威状态或未声明的并行写入方。
14. “持久化原子性”描述必须共同提交的数据库事实，不等同于类、Handler 或事务代码设计；后续实现可以选择技术方式，但不得拆散已经确认的原子边界。
15. 最终用户、管理员、租户、数据范围、领域 Owner 和业务操作权限由用户中心代码默认拒绝地校验；数据库账号不映射这些业务主体和作用域。
16. 基线不依赖 RLS、数据库 Policy、`SECURITY DEFINER`、按租户建账号/Schema 或存储过程承载业务授权，以保持数据库升级、迁移和替换路径可控；少数存储安全特性只能作为防御纵深，不得成为业务正确性的唯一来源。

## 3. 业务模型持久化检查

下表“数据库硬边界”只登记当前 DDL、事务原子性或技术 Grant 能够最终裁决的结构约束。跨表存在性、无法由目标表唯一约束直接覆盖的跨 Range 分区全局唯一/业务幂等、追加/不可变语义、状态转换和业务授权必须登记在“非数据库职责与后续关注”中；审计以及选定高价值追加证据表的技术 Grant 是明确的粗粒度防御纵深，但仍不代替代码契约。

| 业务模型/过程 | 主要持久化载体 | 必须保存的事实 | 数据库硬边界 | 非数据库职责与后续关注 |
|---|---|---|---|---|
| 用户主体 | `global_users` | 稳定 UID、用户类型、生命周期、认证锁定、安全冻结、安全水位、版本和业务时间 | UID 唯一、版本/epoch 非负、CAS 字段 | 三个状态维度的合法转换、冻结影响面、Guest 升级和终态规则 |
| Subject | `user_subjects` | User、Client、Subject、生成版本、退役时间和当前占位 | Client 内历史 Subject 唯一、User/Client 最多一个当前占位、CAS | Pairwise 生成、轮换资格、兼容窗口和对外暴露规则 |
| Identifier | `identifiers`、`identifier_claims`、`identifier_bindings` | 规范化版本、盲索引、验证、占用、绑定、解绑、隔离和原因 | 作用域内唯一占用、版本字段 | 规范化、验证、换绑、隔离期、租户一致性和并发命令 |
| 外部身份 | `user_identities` | 协议、Issuer/Entity ID、稳定外部键摘要、用户关联和元数据 | 稳定外部键唯一 | OIDC/SAML 键计算、防接管和禁止仅凭邮箱合并 |
| 账号合并 | `operations`、`operation_steps`、`user_aliases` | 权威主体、Alias 墓碑、步骤检查点、结果和失败证据 | Alias 唯一、Operation 幂等键 | 冲突处理、补偿、不可逆边界和下游迁移 |
| API 幂等请求 | `idempotency_records` | 调用方作用域、幂等键、请求摘要、响应摘要或 Operation 引用、有效期 | 调用方作用域内幂等键唯一、摘要和有效期基础约束 | 请求规范化、重放结果选择、冲突响应和过期策略 |
| 用户资料 | `user_profiles`、`profile_documents` | 公共资料版本、主联系方式引用、作用域化文档和 Schema 版本 | 用户 Profile 唯一、文档命名空间唯一 | 字段权威、可见性、净化、Schema 校验和脱敏 |
| 认证器 | `authenticators`、`credential_materials`、`password_history` | 类型、状态、保证特征、材料版本、摘要/密文/公开材料和使用时间 | 材料类型版本和 Credential ID 摘要唯一；每条材料至少包含哈希、密文或公开材料之一；版本为正数 | 认证器至少关联一种有效材料；登记、验证、替换、撤销、密码哈希和 AAL 计算 |
| 恢复码 | `recovery_code_batches`、`recovery_codes` | 批次、序号、摘要、状态、过期和消费时间 | 批次内序号唯一、Token 摘要唯一 | 批次轮换、单次消费和旧批次失效 |
| Challenge | `auth_challenges` | 用途、Client、主体/目标、Token 摘要、状态、尝试次数、版本化上下文、过期和消费时间 | Token 摘要唯一、尝试与有效期基础约束、CAS | 上下文 Schema、原事务绑定、验证、防枚举、重发和原子消费 |
| 登录事务 | `login_transactions`、`login_transaction_steps` | 原授权上下文、已完成步骤、风险快照、状态、过期和版本 | 事务步骤唯一、有效期和 CAS | 因子编排、Consent、Step-up、完成条件和禁止未完成签发 |
| 认证上下文和尝试 | `authentication_contexts`、`authentication_attempts` | AAL/acr/amr、策略版本、证据摘要、结果、原因和时延 | 上下文主键、尝试分区主键和必要查询索引 | 认证决策、失败收敛、保证等级和风险解释；尝试只追加由代码命令/仓储契约保护 |
| 身份保证断言 | `identity_assurance_assertions` | 用户、保证等级、提供方、证据摘要、签发/失效时间和状态 | 断言主键、失效时间顺序和 `row_version` 基础约束 | 核验流程、提供方信任、保证等级解释、撤销和最小披露；断言新建、撤销和历史保护由代码契约执行 |
| 平台应用 | `applications` | 所属业务线、应用代码、Owner、环境、状态、接入 Profile、配置引用和版本 | 应用代码唯一、版本非负、CAS | Owner 有效性、接入认证、Profile 选择、下线和影响分析 |
| Client 与资源 | `oauth_clients`、`api_resources`、`oauth_scopes` | Owner、环境、Profile、配置、安全水位、状态和有效期 | Client ID/Audience/Scope 唯一、CAS | 协议校验、Profile 接入认证、Scope 上界和下线流程 |
| 授权码 | `authorization_codes` | Code 摘要、Client、Redirect、PKCE、Scope、状态和过期 | 摘要唯一、有效期、CAS | 单次兑换、重放处置和上下文匹配 |
| Grant | `authorization_grants` | 请求时间、用户、Client、资源、Tenant、Scope、Consent、状态、生效/过期/撤销时间和安全版本 | 版本非负、时间顺序、CAS | `PENDING → ACTIVE/DENIED/EXPIRED`、Scope/Consent 上界和撤销 |
| Session 与设备 | `devices`、`sessions`、`session_participants` | 设备维度、会话状态、绝对/空闲有效期、参与 RP、安全水位和撤销事实 | 会话 ID 唯一、时间与版本基础约束 | 登录态建立、刷新、设备挂失、全局退出和 RP 通知 |
| Refresh Token | `token_families`、`refresh_token_instances` | Family 状态、序号、Token 摘要、签发/使用/过期和轮换结果 | Token 摘要唯一、Family 序号唯一、CAS | 原子轮换、丢包重试识别、重放和家族级撤销 |
| Access Token | `access_token_records` | Token 摘要/JTI、Subject/Actor、委托、Profile、策略、Consent、安全水位、Audience、确认值和有效期 | 分区主键、有效期和非负值基础约束、必要查询索引 | JTI/摘要的跨分区全局唯一由签发代码和高熵生成保证；Token 生成、签名、声明最小化、撤销和资源服务器校验 |
| 撤销 | `revocation_entries` | 目标、原因、生效时间、水位和过期 | 水位非负、逻辑唯一性和索引 | 撤销范围计算、传播、失败关闭和 SLO |
| Tenant 与组织 | `business_lines`、`tenants`、`tenant_domains`、`organizations` | Owner、状态、安全水位、域名、父组织和版本 | Code/Domain 唯一、禁止自引用、CAS | 租户隔离、组织树防环、停用影响和所有权转移 |
| Membership、邀请和组 | `memberships`、`invitations`、`groups`、`group_members` | 成员作用域/状态/当前占位、邀请目标/权限上界、组关系和有效期 | 同一作用域和用户最多一个当前 Membership、邀请有效期、CAS | 邀请单次消费、角色上界、跨租户拒绝、终态释放占位和重新加入新建事实 |
| Consent 与协议 | `agreement_versions`、`agreement_acceptances`、`consent_aggregates`、`consents` | 协议版本、接受证据、目的/类别/接收方、Consent Epoch、授予/撤回事实 | 协议版本和用户-协议版本接受唯一、epoch 非负、必要 CAS | 接受证据只新增和历史版本保护由代码契约执行；合法依据、单独同意、撤回传播和 Grant/订阅联动 |
| 隐私请求 | `privacy_requests`、`legal_holds`、`data_export_artifacts`、`deletion_proofs` | 请求状态、截止期、保留阻断、导出物、删除结果和证明 | 截止期/有效期和数量基础约束、每请求-处理系统删除证明唯一、必要 CAS | 删除证明只新增和历史保护由代码契约执行；身份核验、导出、删除编排、Legal Hold 和下游对账 |
| 角色与策略 | `permissions`、`roles`、`role_permissions`、`*_role_assignments`、`data_scope_definitions`、`policy_versions`、`policy_bindings` | 权限目录、角色与权限追加关系、授予范围/期限、不可变策略版本、绑定和版本 | Code、当前角色权限关系、版本和绑定唯一、有效期和 CAS | 权限上界、职责分离、撤销后新增关系、策略编译、发布和默认拒绝 |
| 关系型授权事实 | `relationship_tuples` | Subject、Relation、Resource、Tenant、有效期、来源和版本 | 关系元组逻辑唯一、有效期和查询索引 | 关系继承、环路/深度、授权求值、跨租户拒绝和撤销传播 |
| 授权决策 | `authorization_decisions` | Subject/Actor、输入摘要、策略/上下文版本、结果、原因、Obligation 和有效期 | 分区主键、有效期和非负值基础约束、必要查询索引 | `decision_id` 跨分区全局唯一和决策只追加由 PDP/仓储契约保证；PDP/PIP 求值、缓存新鲜度、列表前过滤和失败关闭 |
| 联邦和目录同步 | `identity_providers`、`directory_connectors`、`directory_sync_*`、`directory_object_mappings` | IdP/Connector 配置引用、游标、批次结果、外部对象映射和源版本 | Issuer/外部映射唯一、游标 CAS | 协议处理、属性映射、源权威、冲突、墓碑和重试 |
| 风险与限制 | `risk_signals`、`risk_assessments`、`risk_assessment_signals`、`risk_cases`、`security_signals`、`restriction_entries`、`risk_entity_links` | 信号、评估与信号关联、风险等级、处置、案件、限制和证据 | 信号分区主键、评估 ID 和评估-信号关系唯一、置信度和值域基础约束、必要 CAS | 信号 ID 跨分区全局唯一以及信号/评估/关联只追加由生产者和代码契约保证；信号规范化、模型/策略、解释、处置和降级 |
| 机器主体与凭证 | `machine_principals`、`machine_credentials` | Owner、用途、环境、状态、安全水位、凭证类型、指纹、有效期和替代链 | 指纹唯一、一个旧凭证最多被一个新凭证替代、有效期和 CAS | Client 认证、轮换、重叠窗口、失陷和最小权限 |
| 工作负载信任 | `workload_trust_bundle_versions`、`workload_attestations` | 信任包版本/摘要/有效期、证明 Issuer/Audience/Nonce/JTI 摘要、最终结果和原因 | Bundle 版本唯一、验证事实分区主键和必要查询索引 | 验证事实只追加、Nonce/JTI 重放检测由 MACHINE 代码契约保证；信任链、证明过程和短期凭证签发；若过程需恢复则使用 Operation |
| 委托与审批 | `delegations`、`approval_cases`、`approval_actions` | Actor/Subject、权限范围、链深、有效期、请求摘要、审批动作和唯一执行标识 | 执行标识和同审批单-审批人动作唯一、有效期和 CAS | 审批动作只新增和历史保护由代码契约执行；权限交集、循环检测、职责分离、法定人数和单次执行 |
| 密钥、证书与 JWKS | `cryptographic_keys`、`certificates`、`jwks_releases`、`jwks_release_keys` | KMS/HSM 引用、公开材料、算法、用途、有效期、状态和发布版本 | Kid、证书指纹、JWKS 所有者内版本和发布-密钥关系唯一、基础有效期和 CAS | 历史版本不覆写由 KEY 命令/仓储契约保护；密钥生成、签名、验证重叠、紧急撤销和销毁 |
| 配置与安全例外 | `configuration_versions`、`configuration_releases`、`configuration_release_items`、`security_exceptions` | 不可变配置版本、摘要、发布内容、审批引用、环境和例外期限 | 配置版本唯一、内容摘要、发布项唯一、CAS | Schema/语义校验、灰度、激活、回滚、审批和到期收紧 |
| 配额与使用计量 | `resource_quotas`、`usage_records` | 作用域、资源类型、配额版本、计量窗口、用量、来源和业务时间 | 配额键唯一、用量记录幂等键/窗口索引、基础非负约束 | 配额计算、限流处置、聚合对账、超限策略和容量告警 |
| 审计证据 | `audit_events` | Actor、Subject、Tenant、动作、对象、结果、原因、前后摘要、审批和 Trace | 仅追加权限、分区主键、必要索引；普通应用不得更新或删除 | 审计事件构造、全局事件 ID、字段白名单、脱敏、失败关闭和留存策略 |
| 事件和消费 | `outbox_events`、`inbox_messages`、`event_schema_versions`、`consumer_checkpoints` | 标准信封、Payload、Schema 版本、聚合版本、消费结果和水位 | Outbox Event ID 和 Inbox 消费者-事件键在 Hash 分区下全局唯一、Schema 版本唯一、分区与基础检查 | Outbox/Schema 版本只新增和历史保护由代码契约执行；事件生成、兼容性、发布、去重、防乱序和对外 Subject 改写 |
| Webhook | `webhook_subscriptions`、`webhook_signing_keys`、`webhook_deliveries`、`webhook_delivery_attempts`、`event_replay_requests` | 订阅、Endpoint 密文、签名 Key 引用、投递和尝试事实、回放范围 | 订阅 ID 和订阅-接收方 Key 唯一、投递/尝试分区主键、有效期与非负值基础约束 | `delivery_id` 跨分区全局唯一、投递尝试只追加由 EVENT 代码契约保证；所有权验证、SSRF、防重放、签名、退避、停用和回放授权 |
| 消息 | `message_providers`、`message_template_versions`、`message_requests`、`message_delivery_attempts`、`contact_reachability`、`message_suppressions` | Provider/模板版本、目标密文、非秘密参数摘要、短期秘密句柄、发送请求、尝试、可达性和抑制 | 模板版本唯一、消息参数拒绝秘密键、秘密句柄与失效时间成对、请求/尝试分区主键、有效期和非负值基础约束 | `request_id` 跨分区全局唯一由代码保证；投递秘密保存在外部短期秘密存储并仅在 Worker 内存中渲染；请求幂等由 `idempotency_records` 与应用事务裁决；尝试只追加、模板渲染、限速、路由、供应商降级、回执和安全通知策略由代码执行 |
| 迁移 | `legacy_systems`、`legacy_id_mappings`、`migration_batches`、`migration_items`、`migration_change_logs` | 旧系统权威范围、ID 映射、批次/对象状态、检查点和不可变变更日志 | 外部映射和批次内迁移对象唯一、变更日志分区主键、序号为正数、必要 CAS | 变更序号顺序、跨分区幂等和变更日志只追加由 MIG 代码、`idempotency_records` 和仓储契约保证；清洗、冲突、双写、切换、回滚、对账和退役 |
| 用户自助安全中心 | 复用 `identifiers`、`authenticators`、`authentication_attempts`、`devices`、`sessions`、`authorization_grants`、`consents`、`privacy_requests`、`profile_documents` | 自助操作所依赖的权威事实仍归原领域表保存，不建立第二套 SSC 状态 | 不新增重复用户、凭证、会话、Grant 或 Consent 表 | 自助视图、命令编排、最后凭证保护、脱敏活动历史、撤销入口和通知偏好 |

## 4. 共享表与跨域写入权威

“权威域”负责表结构语义、生命周期字段和并发写入规则；“使用域”可以读取、引用或按统一追加契约写入事实，但不得自行定义更弱状态、绕过 CAS/唯一性或并行维护同一事实。若一个共享载体承载多个领域实例，则必须由稳定类型或能力编号确定每一行的唯一业务 Owner。这些是代码模块、应用服务、命令与仓储边界，不要求为每个领域或每一行建立对应的数据库角色、RLS Policy 或动态 Grant。

| 共享持久化载体 | 权威域 | 使用域 | 写入边界 |
|---|---|---|---|
| `operations`、`operation_steps` | OPS | API 及所有需要跨事务恢复的领域 | OPS 拥有模型和状态字段；每个 Operation 由 `capability_code/operation_type` 确定唯一业务 Owner，只有该 Owner 可以推进对应实例，其他领域不得并行改写状态或步骤。 |
| `user_identities` | ID | FED、AUTH、SSC | ID 持有身份链接生命周期；FED 提供外部身份源事实并消费链接结果，不得直接形成第二套链接、解绑或防接管状态。 |
| `identifiers`、`identifier_bindings` | ID | SSC、AUTH、PROFILE | ID 持有标识、验证和绑定生命周期；使用域只能引用当前有效事实或提交受控命令，不得另建联系方式占用状态。 |
| `login_transactions`、`authentication_contexts` | AUTH | OAP、ASR | AUTH 持有登录事务和认证上下文；OAP 使用其完成协议交互，ASR 使用其计算保证等级，均不得跳过未完成事务或改写认证证据。 |
| `oauth_clients`、`api_resources`、`oauth_scopes` | OAP | PLT、CTRL、API、SESSION | OAP 持有协议对象、安全水位和生命周期；PLT 负责接入编排，CTRL 负责配置审批，使用域不得绕过 OAP 直接改变有效配置或状态。 |
| `authorization_grants` | OAP | SESSION、SSC、PRIV | OAP 持有 Grant 生命周期；SESSION/SSC/PRIV 只能查询、请求撤销或消费撤销结果，不得直接形成另一套授权状态。 |
| `approval_cases`、`approval_actions` | CTRL | ASR、OPS 及所有高风险操作领域 | CTRL 持有审批单和动作证据；发起域保存请求摘要/资源版本并消费审批结果，不得自行写入审批结论或执行终态。 |
| `revocation_entries` | SESSION | OAP、AUTH、KEY、PRIV、RISK | SESSION 持有统一撤销事实和查询语义；使用域提交撤销目标与原因并消费水位，不得建立独立 Denylist 或改写已有撤销记录。 |
| `devices`、`sessions`、`session_participants` | SESSION | SSC、OAP | SESSION 持有设备和会话生命周期；SSC 只展示和提交退出命令，OAP 只引用会话事实，不得复制登录态或直接放宽撤销结果。 |
| `authenticators`、`credential_materials`、`password_history`、`recovery_code_batches`、`recovery_codes`、`authentication_attempts` | AUTH | ASR、SSC | AUTH 持有认证器、凭证材料、恢复码和认证尝试；ASR/SSC 只能发起受控恢复或展示命令，不得直接降低保证等级或修改材料状态。 |
| `identity_assurance_assertions` | PROFILE | ASR、AUTH、PRIV | PROFILE 持有保证断言及撤销事实；ASR 定义和使用保证等级模型，其他使用域只引用有效断言和最小证据，不得自行提升断言等级。 |
| `user_profiles`、`profile_documents` | PROFILE | SSC、PRIV | PROFILE 持有资料版本和字段权威；使用域按授权读取或发起隐私处理，不得建立不可对账的资料副本。 |
| `consents`、`privacy_requests`、`data_export_artifacts` | PRIV | SSC、OAP、PROFILE | PRIV 持有 Consent、隐私请求和导出物生命周期；使用域只能展示、引用或提交受控请求，不得改写合法依据与撤回状态。 |
| `configuration_versions`、`configuration_releases`、`configuration_release_items` | CTRL | PLT、OAP、RISK、KEY、MSG、FED 及其他配置消费域 | CTRL 持有版本、审批和发布状态；业务域可以生成候选或验证结果，但不得原地修改已发布版本或绕过发布指针。 |
| `business_lines` | TENANT | PLT、OAP、AUTHZ | TENANT 持有业务线生命周期；使用域只引用作用域和安全水位，不得建立重复业务线台账。 |
| `user_role_assignments`、`group_role_assignments`、`machine_role_assignments` | AUTHZ | TENANT、MACHINE | AUTHZ 持有角色授予关系、范围和有效期；主体领域提供被授权对象事实，不得直接形成绕过策略与审计的第二套角色关系。 |
| `applications`、`resource_quotas`、`usage_records` | PLT | OAP、OBS 及业务接入域 | PLT 持有应用、配额和使用计量模型；OBS 读取或聚合观测，业务域只按幂等计量契约提交用量，不得修改历史记录。 |
| `audit_events` | OBS | 所有产生审计事实的领域 | OBS 持有 Schema、权限、分区和留存；各领域只能通过审计 Writer 追加自身动作事实，任何普通业务角色不得更新或删除。 |
| `outbox_events`、`inbox_messages` | EVENT | 所有事件生产者和消费者 | EVENT 持有信封、去重、分区和保留契约；生产者在本地权威事务中追加 Outbox，消费者按统一契约写 Inbox，不得改写其他领域事件或消费记录。 |

未列入本表且只出现在一个领域持久化范围内的表，其权威域即对应业务模型所属领域。新增共享表或新增第二使用域时，必须先更新本节再修改 SQL。

## 5. 模型变更判定

新增业务要求时，按以下顺序判断是否需要调整数据库：

1. 是否产生新的独立生命周期对象或需要长期审计的事实；若否，优先扩展现有模型。
2. 是否需要按该值高频过滤、唯一约束、建立逻辑关系或执行安全水位判断；若是，使用独立字段，不隐藏在 JSONB。
3. 是否只是可重算决策、中间计算结果或代码流程；若是，不建立永久表。
4. 是否跨越同步事务边界并需要恢复；若是，复用 `operations + operation_steps`。
5. 是否是历史版本；若是，新增不可变版本，不原地改写已发布内容。
6. 是否需要数据库最终解决并发竞争；只有唯一性、CAS、单次消费和必要原子写入进入数据库硬边界，其余业务判断留给后续实施。

## 6. 与代码实施契约的关系

代码实施必须基于本文件和 `docs/代码实施`，补充具体接口、类、外部依赖和自动化测试。实现方式可以调整，但必须满足以下约束：

- 不新增与本文件重复的权威事实。
- 不绕过数据库唯一性、CAS 和已明确的技术访问隔离；不绕过代码中的业务授权、领域 Owner 和追加/不可变写入边界。
- 不把状态机、授权、风险、审批或跨系统工作流下沉为数据库 Trigger/Routine。
- 发现业务模型无法表达需求时，先回到数据库模型评审，不通过临时 JSON 字段或影子表规避。

## 7. 领域持久化边界

本节由原 21 个独立领域文档合并而来，按能力域登记持久化范围、权威边界和验收门禁。生成脚本从各三级标题及其“持久化范围”行解析领域映射；新增或改名领域时必须同步通过数据库文档生成校验。

### API：通用 API 契约

- 持久化范围：`idempotency_records`、`operations`、`operation_steps`。
- 业务模型边界：`IdempotentRequest`、`Operation`。
- 权威边界：API 持有 `idempotency_records`；`operations + operation_steps` 的模型由 OPS 持有，每个实例由创建它的能力域唯一推进，API 不得形成第二套 Operation 状态。
- 业务模型要求：持久化模型必须支持统一错误码、`Idempotency-Key`、ETag/CAS、不透明分页、异步 `202 + operation_id`、逐项批量结果和协议错误映射；Operation 创建时固化调用作用域、幂等键、请求摘要、能力编号、Saga 类型与策略版本。
- 授权：查询、取消、继续和人工接管 Operation 时重新校验 Actor、Subject、Tenant 和能力权限。
- 事件：Operation 状态变化；不得把内部敏感错误直接进入响应或事件。
- 门禁：`API-G-001~019`、`AT-API-001~005`；同调用方幂等键不得绑定两个 Operation，创建快照不得被后续步骤改写。

### ASR：保证、恢复、委托与代理

- 持久化范围：`authentication_contexts`、`identity_assurance_assertions`、`authenticators`、`recovery_code_batches`、`recovery_codes`、`delegations`、`approval_*`。
- 业务模型边界：`AssuranceContext`、恢复 `Operation`、`Delegation`、`ApprovalCase`。
- 权威边界：ASR 持有保证等级规则和 `delegations`；`identity_assurance_assertions` 由 PROFILE 持有，认证上下文、认证器与恢复码由 AUTH 持有，审批单与审批动作由 CTRL 持有，恢复 Operation 遵守 OPS 的实例唯一 Owner 规则。
- 业务模型要求：持久化模型必须支持恢复不降低保证等级、认证器替换风险保护、Actor/Subject 分离、委托权限交集/链深/撤销和未成年人或代理规则；审批执行必须绑定唯一 `execution_id`。
- 禁止：同步型 Passkey 误评 AAL3；找回绕过 MFA；代理扩权；发起人自审。
- 事件：恢复进度、认证器替换、委托生效/撤销和审批结果。
- 门禁：`CAP-ASR-*`、`AT-AUTH-004~007/015`、`AT-ASR-*`、`AT-MACHINE-004`；同一审批并发执行只能成功一次。

### AUTH：认证器与认证

- 持久化范围：`authenticators`、`credential_materials`、`password_history`、`recovery_code_batches`、`recovery_codes`、`auth_challenges`、`login_transaction*`、`authentication_contexts`、`authentication_attempts`。
- 业务模型边界：`Authenticator`、`Challenge`、`LoginTransaction`、`AuthenticationContext`。
- 权威边界：AUTH 持有认证器、认证材料、恢复码、Challenge、登录事务、认证上下文和尝试事实；ASR/SSC 只能通过受控命令使用这些事实，不得直接改写凭证生命周期。
- 业务模型要求：持久化模型必须为算法 Allowlist、密码自适应哈希、Challenge 单次消费、尝试限制、防枚举、MFA/Passkey、AAL 计算和风险 Step-up 保存所需事实与版本。
- 禁止：密码/验证码/TOTP 原文落库或进入日志、事件；冻结用户登记认证器；未完成 Login Transaction 签发。
- 事件：认证器登记/替换/撤销、认证成功/失败安全事件、登录事务完成。
- 门禁：`AT-AUTH-*`、`INV-G-007/013/016`，包含并发消费和保证等级决策表。

### AUTHZ：授权与策略

- 持久化范围：`permissions`、`roles`、`role_permissions`、`user_role_assignments`、`group_role_assignments`、`machine_role_assignments`、`data_scope_definitions`、`policy_versions`、`policy_bindings`、`authorization_decisions`、`relationship_tuples`。
- 业务模型边界：`Role`、`Assignment`、`PolicyVersion/Binding`、关系元组；决策为不可变证据。
- 业务模型要求：持久化模型必须支持默认拒绝、作用域包含、最小权限、职责分离、Subject/Actor/委托、PDP/PIP、属性版本与新鲜度、决策有效期、列表查询前过滤、缓存水位和 Obligation。
- 禁止：数据库 View 表达“有效授权”；前端、数据库账号/角色或 RLS/Policy 作为业务授权的权威/唯一 PEP；受保护仓储在缺少可信授权上下文时继续读写；依赖过期缓存继续高风险放行。
- 事件：角色/权限/策略/关系变化和安全水位推进。
- 门禁：`AT-AUTHZ-*`、`AT-FAIL-001`、租户隔离、领域 Owner 越权、受保护数据访问绕过、追加/不可变历史篡改和撤销时效测试；改变 Actor、风险、资源版本、PIP 版本或 Consent Epoch 不得命中旧允许缓存。

### CTRL：控制面、审批与安全例外

- 持久化范围：`configuration_versions`、`configuration_releases`、`configuration_release_items`、`approval_cases`、`approval_actions`、`security_exceptions`。
- 业务模型边界：不可变 `ConfigurationVersion`、`ConfigurationRelease`、`ApprovalCase`、`SecurityException`。
- 权威边界：CTRL 持有配置版本/发布、审批单/动作和安全例外；其他领域可以提交候选、验证结果和审批请求，但不得直接激活版本、写入审批结论或延长例外。
- 业务模型要求：持久化模型必须支持 Schema/语义验证、Seed 内容摘要与漂移检测、差异、审批、职责分离、资源版本绑定、唯一执行标识、灰度、回滚和例外到期收紧。
- 禁止：DRAFT/未审批配置激活；Seed 同版本静默漂移；发起人自审；批准后修改请求；超期例外继续生效。
- 事件：配置发布/回滚、审批终态、例外生效/到期。
- 门禁：`AT-CTRL-*`、`AT-ASR-003`、根密钥与首个管理员恢复演练。

### EVENT：事件、Webhook 与消费水位

- 持久化范围：`outbox_events`、`inbox_messages`、`event_schema_versions`、`webhook_subscriptions`、`webhook_signing_keys`、`webhook_deliveries`、`webhook_delivery_attempts`、`event_replay_requests`、`consumer_checkpoints`。
- 业务模型边界：`EventSchemaVersion`、`WebhookSubscription`、`ReplayRequest`；投递为队列事实。
- 权威边界：EVENT 持有事件信封、去重、Schema、Webhook 和消费水位契约；各业务域只在自己的权威事务中追加 Outbox 或按统一契约写 Inbox，不得改写其他领域事件与消费事实。
- 业务模型要求：持久化模型必须支持至少一次、Inbox 去重、聚合版本防乱序、核心事件 Schema、Producer 授权、完整事件信封、显式 Subject/Actor 标识类型、敏感级别、pairwise Subject、Webhook 验签/重放窗/SSRF 和受控回放。
- 分区：Outbox/Inbox Hash 保证全局去重；投递和尝试按月 Range。
- 禁止：把核心信封隐藏在 `headers`；事件默认携带 PII/Global UID；未授权 Producer；回放重复不可逆副作用。
- 门禁：`EVT-G-*`、`AT-EVENT-*`、断档补拉和 DNS 重绑定负向测试。

### FED：身份联合与企业目录

- 持久化范围：`identity_providers`、`directory_connectors`、`directory_sync_cursors`、`directory_sync_batches`、`directory_object_mappings`、`user_identities`、`legacy_systems`、`legacy_id_mappings`、`migration_batches`、`migration_items`、`migration_change_logs`。
- 业务模型边界：`IdentityProvider`、`DirectoryConnector`、同步 `Operation/Batch`、存量身份源与凭证迁移 `MigrationSystem/Batch`。
- 权威边界：FED 持有身份源、目录连接器、同步游标/批次、外部对象映射及 `CAP-FED-012/013` 对应的迁移模型；`user_identities` 的链接生命周期由 ID 持有，FED 只提交外部身份源事实并消费链接结果。
- 业务模型要求：持久化模型必须支持 OIDC `issuer+sub`、SAML 完整限定元组、禁止仅凭邮箱合并、路由和属性映射版本化以及 SCIM/API 游标、ETag、墓碑和冲突。
- 秘密：Client Secret、第三方 Token 和私钥只存 KMS/Vault；数据库保存配置/Key 引用。
- 事件：身份链接、目录对象创建/更新/墓碑、同步批次结果。
- 门禁：`AT-FED-*`、协议负向测试、旧版本覆盖和跨租户目录映射测试。

### ID：身份主体与标识

- 持久化范围：`global_users`、`user_subjects`、`identifiers`、`identifier_claims`、`identifier_bindings`、`user_identities`、`user_aliases`。
- 业务模型边界：`GlobalUser`、`IdentifierClaim`、`ExternalIdentityLink`、账号合并 `Operation`。
- 权威边界：ID 持有用户、Subject、Identifier、外部身份链接和 Alias 墓碑；FED 不得直接改变 `user_identities` 的链接、解绑或防接管状态，账号合并 Operation 遵守 OPS 的实例唯一 Owner 规则。
- 业务模型要求：持久化模型必须支持 UUID/公开 ID 不复用、Subject 轮换历史和单一当前占位、正式/Guest 类型与到期升级、生命周期/认证锁定/安全冻结正交、版本化规范化、标识并发唯一占用、解绑隔离、合并补偿和匿名化终态。
- 持久化原子性：主体/标识事实、唯一占用、Outbox、Audit、幂等结果必须共同提交。
- 事件：用户创建/冻结/恢复/终态，标识验证/绑定/解绑，身份链接/解绑，账号合并。
- 门禁：`AT-ID-*`、`INV-G-001~005/009/013`；同标识双绑定、Guest 到期继续签发、锁定/冻结维度互相覆盖、旧 UID 复用和匿名化恢复必须失败。

### KEY：密钥与证书

- 持久化范围：`cryptographic_keys`、`certificates`、`jwks_releases`、`jwks_release_keys`。
- 业务模型边界：`KeyMetadata`、`Certificate`、`JwksRelease`。
- 业务模型要求：数据库只保存 KMS/HSM 引用和公开材料，并为用途隔离、算法 Allowlist、签名与验证重叠窗口、紧急阻断和销毁证明保存所需版本与事实。
- 禁止：私钥、可导出 Secret 和完整凭证明文落库；未批准算法投入生产。
- 事件：Key 生成/激活/轮换/撤销、JWKS 发布/退役、证书失陷。
- 门禁：`AT-KEY-*`、双轮换、旧 Token 验证、盲索引不可枚举和失陷演练。

### MACHINE：机器与工作负载身份

- 持久化范围：`machine_principals`、`machine_credentials`、`workload_trust_bundle_versions`、`workload_attestations`、`machine_role_assignments`。
- 业务模型边界：`MachinePrincipal`、`MachineCredential`、`TrustBundleVersion`。
- 权威边界：MACHINE 持有机器主体、机器凭证、信任包和证明事实；`machine_role_assignments` 的授权关系由 AUTHZ 持有，MACHINE 只提交主体用途、环境与最小权限约束。
- 业务模型要求：持久化模型必须支持负责人、用途、环境、最小权限、强制到期、private_key_jwt/mTLS、明确 Audience、短 Token、自动轮换、替代凭证链、失陷响应、工作负载证明和重放检测。
- 禁止：长期无主机器账号、共享凭证明文、跨环境凭证和无 Audience 的证明。
- 事件：主体/凭证状态、Trust Bundle 发布、证明失败和失陷处置。
- 门禁：`AT-MACHINE-*`、SP4 不签发 Refresh Token、台账差异、轮换窗口、Nonce/JTI 重放和紧急阻断测试。

### MSG：消息投递

- 持久化范围：`message_providers`、`message_template_versions`、`message_requests`、`message_delivery_attempts`、`contact_reachability`、`message_suppressions`。
- 业务模型边界：`MessageRequest`、`ContactReachability`、`MessageSuppression`；模板版本不可变。
- 业务模型要求：持久化模型必须支持目标加密、目的限速、模板变量 JSON Schema、模板审批发布、抑制优先级、多供应商路由、回执和成本归一；安全转义和发送策略属于非数据库职责。
- 禁止：供应商故障时绕过认证；IAM 持久化参数、日志、事件或审计包含验证码、Magic Link Token 或完整 Token；秘密句柄成为脱离 Worker 身份即可使用的承载凭证；营销退订失效。
- 事件：发送结果、可达性变化、抑制变化和供应商健康。
- 门禁：通道故障演练、幂等发送、硬退信/投诉、`required` 变量必须存在于 `properties`、模板注入和敏感输出扫描。

### OAP：OAuth/OIDC Provider

- 持久化范围：`oauth_clients`、`api_resources`、`oauth_scopes`、`login_transactions`、`authorization_codes`、`authorization_grants`、`token_families`、`refresh_token_instances`、`access_token_records`、`revocation_entries`。
- 业务模型边界：`Client`、`AuthorizationGrant`、`TokenFamily`；Code/Token 记录为单次或追加事实。
- 权威边界：OAP 持有 Client、API Resource、Scope、授权码、Grant 和 Token 事实；`login_transactions` 由 AUTH 持有，PLT 只负责编排接入，CTRL 只负责配置审批，`revocation_entries` 由 SESSION 持有，OAP 提交撤销原因并消费统一水位。
- 业务模型要求：持久化模型必须支持 Authorization Code + PKCE、精确 Redirect、Client 认证、Scope/Consent、Device Flow、Token 签发/验证、Refresh 轮换和撤销；签发记录固化 Subject/Actor/委托链、Profile、策略、Consent、水位、Audience 和 DPoP/mTLS 确认值。
- 禁止：ROPC/自定义密码轮询；未完成登录事务签发；Token 原文落库；风险升高时静默降级。
- 事件：Grant/Token Family/Client 安全状态变化和撤销水位。
- 门禁：`REQ-OAP-*`、`AT-OAP-*`、`AT-SESSION-*` 和协议一致性套件；SP4 禁止 Refresh Token，SP5 必须 PAR 和发送方约束。

### OBS：审计、可观测性与 SLO

- 持久化范围：`audit_events`、`usage_records`、各类追加事实表，以及 `SLO_BASELINE` 配置。
- 业务模型边界：审计为不可变事件；指标、日志和 Trace 主要在可观测性设施，不把时序平台复制成业务表。
- 权威边界：OBS 持有 `audit_events` 的 Schema、权限、分区和留存契约；各领域只可追加自身审计事实。`usage_records` 由 PLT 持有，OBS 只读取、聚合和对账，不改写历史用量。
- 业务模型要求：数据库保存审计和稳定证据；结构化日志、Trace 传播、审计失败关闭、敏感数据扫描、SLO/错误预算和告警路由属于非数据库职责，但所需对象引用、原因码和时间必须可持久化。
- 权限：审计 Writer 仅 INSERT，Reader 仅 SELECT，普通应用不可更新或删除。
- 事件：安全告警和运维事件必须引用稳定原因码和 Trace，不含密码/验证码/Token。
- 门禁：`AT-AUDIT-*`、`AT-SEC-*`、`AT-SLO-*`、告警和值班演练。

### OPS：跨域操作与运营处置

- 持久化范围：`operations`、`operation_steps`、`approval_cases`、`audit_events`。
- 业务模型边界：通用 Saga `Operation`，不为注销、合并、换绑、迁移分别建数据库工作流。
- 权威边界：OPS 持有 Operation 模型；每个实例由 `capability_code/operation_type` 确定唯一业务 Owner。`approval_cases` 由 CTRL 持有，`audit_events` 的追加契约由 OBS 持有，OPS 不得并行改写两者的状态。
- 业务模型要求：`operations + operation_steps` 只保存检查点、幂等步骤状态、不可逆边界、超时、结果和证据；补偿、前向修复与人工接管流程不在数据库中实现。
- 持久化原子性：每一步只共同提交本地权威事实和下一步 Outbox；不使用分布式数据库事务。
- 禁止：越过不可逆边界后伪装回滚；重复执行已完成副作用。
- 门禁：故障注入、宕机恢复、检查点继续和跨主体/租户 Operation 越权测试。

### PLT：平台接入与运行基座

- 持久化范围：`business_lines`、`applications`、`oauth_clients`、`api_resources`、`usage_records`、`resource_quotas`、配置发布表。
- 业务模型边界：`Application`、Client 上线认证 `Operation`、配额和使用计量。
- 权威边界：PLT 持有 `applications`、`resource_quotas` 和 `usage_records`；`business_lines` 由 TENANT 持有，Client/API Resource 由 OAP 持有，配置版本/发布由 CTRL 持有，PLT 只编排接入和消费发布结果。
- 业务模型要求：持久化模型必须保存所有者、环境、Security Profile、接入结果、配额和发布证据；容量、备份恢复与发布门禁的执行属于非数据库职责。
- 禁止：Seed 创建生产 Client/管理员/秘密；业务系统直接获得 IAM 数据库账号。
- 事件：应用/Client 状态、配置发布、配额告警和接入认证结果。
- 门禁：`CAP-PLT-*`、协议一致性、状态机属性、密钥轮换、租户隔离和 DR 演练。

### PRIV：协议、Consent 与隐私权利

- 持久化范围：`agreement_versions`、`agreement_acceptances`、`consent_aggregates`、`consents`、`privacy_requests`、`legal_holds`、`data_export_artifacts`、`deletion_proofs`。
- 业务模型边界：`ConsentAggregate`、`PrivacyRequest`、`LegalHold`；协议接受和删除证明不可变。
- 业务模型要求：持久化模型必须支持明确目的/类别/接收方、单独同意、撤回水位、法定期限、代理核验、导出加密、删除编排和依法保留所需的权威事实与证明。
- 禁止：Consent 撤回后继续签发/订阅；Legal Hold 下删除；删除终态主体恢复。
- 事件：Consent 授予/撤回、隐私请求进度、导出就绪、删除证明完成。
- 门禁：`AT-PRIV-*`、下游传播对账、备份保留核查和跨境/敏感信息规则测试。

### PROFILE：用户资料与身份保证

- 持久化范围：`user_profiles`、`profile_documents`、`identity_assurance_assertions`。
- 业务模型边界：稳定 `UserProfile`、作用域化 `ProfileDocument`、不可变保证断言。
- 权威边界：PROFILE 持有用户资料、作用域化文档和 `identity_assurance_assertions`；ASR 使用断言计算保证等级，其他领域只引用有效断言并执行最小披露。
- 业务模型要求：持久化模型必须支持字段级权威、JSON Schema、可见性、版本化 Claim 映射和脱敏；主联系方式必须引用当前用户已验证且有效绑定的 Identifier，输入净化属于非数据库职责。
- 删除：隐私流程按数据目录删除或匿名化；保证断言只保留最小证据和摘要。
- 事件：资料版本变化、保证断言签发/撤销；对外事件使用接收方 Subject。
- 门禁：`CAP-PROFILE-*`、主联系方式跨用户/未验证/已解绑负向测试、隐私字段授权和乱序事件测试。

### RISK：风险与安全状态

- 持久化范围：`risk_signals`、`risk_assessments`、`risk_assessment_signals`、`risk_cases`、`security_signals`、`restriction_entries`、`risk_entity_links`。
- 业务模型边界：不可变 `RiskAssessment`、`RiskCase`、`RestrictionEntry`。
- 业务模型要求：持久化模型必须保存信号 Schema、模型/策略版本、解释码、风险等级、处置、限制优先级、风险关联图边界和数据保留所需事实；风险计算本身不进入数据库。
- 降级：模型、特征、撤销水位不可确定时按版本化失败策略 Step-up 或拒绝。
- 事件：风险等级变化、案件开启/关闭、安全信号和限制生效/解除。
- 门禁：`AT-RISK-*`、管理员/恢复/换绑/合并/机器身份场景覆盖与故障注入。

### SESSION：设备、会话与撤销

- 持久化范围：`devices`、`sessions`、`session_participants`、`revocation_entries`，并引用 Token/Grant 表。
- 业务模型边界：`Device`、`Session`、全局退出 `Operation`。
- 权威边界：SESSION 持有设备、会话和统一撤销事实；Token/Grant 由 OAP 持有，SESSION 不直接改写其生命周期，而通过统一撤销事实和安全水位约束后续签发与验证。
- 业务模型要求：持久化模型必须支持空闲/绝对过期、设备信任、冻结和安全水位、Profile/策略/Consent/撤销水位快照、并发会话、RP 前后通道退出和撤销失败关闭所需事实。
- 持久化原子性：撤销 Session/Grant/Token 的权威事实、安全水位递增、Outbox 和 Audit 必须共同提交。
- 事件：会话创建/刷新/撤销、设备信任变化、全局退出进度。
- 门禁：`AT-SESSION-*`、`SLO-REVOKE-*`；刷新与冻结、Consent 撤回、委托撤销竞争时不得产生新有效 Token，SP2/SP3/SP5 水位不确定时失败关闭。

### SSC：用户自助账号与安全中心

- 持久化范围：复用 `identifiers`、`identifier_bindings`、`authenticators`、`authentication_attempts`、`audit_events`、`devices`、`sessions`、`authorization_grants`、`consents`、`privacy_requests`、`data_export_artifacts`、`profile_documents` 和 `operations`；不建立第二套 SSC 用户、凭证、会话、Grant 或 Consent 表。
- 业务模型：SSC 是用户侧查询和命令入口，不拥有重复的权威聚合；登录方式、设备/会话、应用授权、Consent、导出和注销继续由原领域模型持久化。
- 业务模型要求：持久化事实必须支持登录方式管理、最后凭证保护、安全活动历史、设备和会话退出、应用授权撤回、可疑活动上报、协议/Consent 查看、数据导出、注销进度、安全建议和通知偏好。
- 权威边界：标识由 ID 持有，认证器与认证尝试由 AUTH 持有，设备/会话由 SESSION 持有，Grant 由 OAP 持有，Consent/隐私请求/导出物由 PRIV 持有，资料文档由 PROFILE 持有，审计契约由 OBS 持有，Operation 模型由 OPS 持有；SSC 不得直接复制或形成更弱的状态与撤销模型。
- 禁止：为页面展示建立不可对账的影子状态；把脱敏活动时间线当作完整审计证据；允许移除最后一个可用登录方式后失去全部恢复路径。
- 事件：复用各权威领域事件；可疑活动上报产生风险信号或受控 Operation，不新增语义重复事件。
- 门禁：`CAP-SSC-*`；验证最后凭证保护、跨用户数据隔离、单个/全部退出、授权撤回、Consent/协议展示、导出与注销进度，以及安全通知不可完全关闭。

### TENANT：业务线、租户、组织与成员

- 持久化范围：`business_lines`、`tenants`、`tenant_domains`、`organizations`、`memberships`、`invitations`、`groups`、`group_members`。
- 业务模型边界：`BusinessLine`、`Tenant`、`Organization`、`Membership`、`Invitation`、`Group`。
- 权威边界：TENANT 持有业务线、租户、组织、成员、邀请和组关系；PLT/OAP/AUTHZ 只引用其作用域与安全水位，不得建立重复业务线或租户状态。
- 业务模型要求：持久化模型必须支持全局/租户/Membership 状态正交、Membership 终态历史与重新加入新记录、组织树防环、邀请目标和权限上界以及全链路租户隔离所需事实。
- 禁止：仅靠前端或查询后过滤实现隔离；以 RLS/Policy、租户 Schema 或租户数据库账号作为唯一租户隔离来源；跨租户父组织、组成员或角色授予。
- 事件：租户状态、组织变化、成员加入/离开、邀请消费、组成员变化。
- 门禁：`AT-TENANT-*`、跨租户搜索/缓存/事件/批量接口负向测试；未配置可选 RLS/Policy 时代码侧隔离结果不变。
