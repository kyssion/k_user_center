# 统一身份与访问平台（IAM/CIAM）能力地图

> 文档状态：目标态完整能力地图
> 适用范围：集团或平台内多个业务线共用统一用户身份、登录认证与访问控制能力
> 核心目标：一次建设、多业务复用；统一身份、隔离业务；安全可控、标准接入、持续演进
> 配套蓝图：[统一身份与访问平台建设与验收蓝图](./统一身份与访问平台建设与验收蓝图.md)

## 1. 平台定位

统一身份与访问平台不是一张公共用户表，也不只是登录注册服务，而是所有业务系统共同依赖的身份基础设施。

平台负责：

- 建立全局、稳定、不可变的用户主体。
- 管理手机号、邮箱、密码、第三方账号、企业账号、Passkey 等登录身份。
- 向 Web、App、小程序、服务端和第三方系统提供统一认证。
- 管理登录会话、令牌、单点登录和账号安全。
- 管理公共用户资料、隐私授权和账号生命周期。
- 提供业务线、租户、组织、成员关系及统一授权能力。
- 提供管理后台、审计、风控、事件通知、SDK 和开放 API。

平台原则上不负责：

- 订单、资产、会员等级、积分、内容、客户标签等具体业务数据。
- 替代 CRM、会员系统、组织人事系统或业务后台。
- 把所有业务数据集中到用户中心。
- 允许业务系统直接访问用户中心数据库。

### 1.1 建设原则

1. **身份与业务解耦**：全局用户只表达“是谁”，业务成员关系表达“在哪个业务中是什么状态”。
2. **认证与授权分离**：认证解决“是谁”，授权解决“能做什么、能访问哪些数据”。
3. **标识与登录名分离**：手机号、邮箱会变化，不能作为用户主键。
4. **公共资料与业务资料分离**：公共信息统一管理，业务私有信息由业务系统负责。
5. **全局状态与业务状态分离**：安全冻结可全局生效，业务封禁只能影响指定业务。
6. **协议优先**：优先使用 OpenID Connect、OAuth 2.0、WebAuthn、SCIM 等标准能力。
7. **默认最小权限**：业务线、管理员、应用和服务只获得完成任务所需的最小权限。
8. **全程可审计**：身份、凭证、权限和管理操作必须留痕。
9. **避免强耦合**：通过标准协议、API 和事件集成，不共享表、不依赖内部实现。
10. **安全与可用性并重**：身份平台属于 Tier-0 基础设施，必须考虑高可用、容灾和应急处置。
11. **保证等级驱动**：身份核验、认证和联合保证等级必须显式建模，敏感操作按等级和认证新鲜度执行。
12. **状态机与不变量优先**：所有关键对象必须定义合法转换、并发语义和不可破坏的不变量。
13. **端到端撤销**：冻结、改密、离职、权限回收和风险事件必须传播到会话、令牌、应用和下游缓存。
14. **机器身份同等治理**：服务账号、工作负载、Client 和代理调用必须具备负责人、最小权限、到期和轮换。
15. **控制面与数据面分离**：配置、策略、密钥和应用接入必须版本化、审批、灰度、回滚并与在线认证隔离。
16. **持续验证而非一次验收**：协议一致性、不变量、租户隔离、密钥轮换、容灾和攻击负向用例必须自动化验证。
17. **恢复路径不降级**：找回、客服处置、管理员恢复和应急操作不得成为绕过 MFA 或审批的低保证后门。

## 2. 总体能力地图

~~~mermaid
mindmap
  root((统一身份与访问平台))
    身份主体
      全局用户
      登录身份
      凭证
      身份关联
      标识规范化与回收
      业务成员关系
      账号生命周期
      身份与认证保证等级
      机器身份
    注册与认证
      注册
      密码与验证码
      第三方登录
      企业联合登录
      MFA
      Passkey
      找回与二次认证
      认证器生命周期
      高保证账号恢复
    会话与令牌
      SSO
      会话管理
      Token签发
      Token吊销
      授权Grant
      Refresh Token家族
      设备管理
      密钥轮换
      前后通道退出
    用户资料
      公共资料
      业务扩展资料
      头像
      联系方式
      偏好设置
      字段治理
    组织与多租户
      业务线
      应用
      租户
      组织部门
      用户组
      成员与邀请
    权限与策略
      RBAC
      数据范围
      ABAC
      关系授权
      策略决策
      PAP/PDP/PEP/PIP
      决策契约与义务
      权限审计
    安全与风控
      防暴力破解
      设备与行为风险
      验证码安全
      管理员安全
      密钥与加密
      安全响应
      全生命周期欺诈
      持续访问评估
    隐私与治理
      用户协议
      隐私同意
      数据最小化
      导出更正删除
      保留与匿名化
      合规审计
      隐私请求编排
      法律保留与删除证明
    开放平台
      标准协议
      API
      SDK
      Webhook
      事件总线
      应用自助接入
      OAuth/OIDC安全Profile
      事件签名与防重放
      协议一致性认证
    运营与管理
      用户查询
      冻结解冻
      合并拆分
      客服处置
      审批
      审计时间线
    平台工程
      高可用
      容灾
      监控告警
      容量治理
      灰度发布
      数据迁移
      控制面与配置治理
      自动化契约测试
      工作负载身份基础设施
~~~

## 3. 核心领域模型

### 3.1 核心对象

| 对象 | 定义 | 关键要求 |
|---|---|---|
| Global User | 平台内唯一的用户主体 | UID 永久、不可变、无业务含义、不可复用 |
| Identity | 用户可用于认证的身份 | 手机、邮箱、用户名、微信、Apple、企业账号等，一个用户可有多个 |
| Credential | 证明身份的凭证 | 密码哈希、Passkey 公钥、TOTP 密钥等，与用户资料隔离 |
| Authenticator | 用户实际持有或控制的认证器 | 记录类型、保证等级、状态、登记、替换、挂失、撤销和恢复生命周期 |
| Identifier | 可被识别或检索的标识 | 手机号、邮箱、外部账号 ID；需记录验证状态、归属和历史 |
| Application | 接入用户中心的应用 | 维护 Client、回调地址、协议、密钥、作用域和环境 |
| Business Line | 业务域或产品线 | 作为应用、权限、数据隔离和管理授权边界 |
| Tenant | 企业客户或隔离单元 | 支持租户级账号策略、数据隔离和管理员委派 |
| Organization | 租户内组织结构 | 公司、部门、门店、项目组等 |
| Membership | 用户与业务线、租户或组织的关系 | 保存业务状态、加入来源、业务角色、业务昵称等 |
| Profile | 用户资料 | 分为平台公共资料和业务扩展资料 |
| Session | 一次持续登录关系 | 关联设备、应用、认证强度、过期时间和风险状态 |
| Authorization Grant | 主体授予 Client 的访问关系 | 关联资源、scope、同意、有效期、撤回和策略版本 |
| Token Family | 一组可轮换的 Refresh Token 关系 | 支持重放检测、家族级吊销、设备和 Client 绑定 |
| Token | 应用访问凭证 | 有 audience、scope、有效期、签发方和吊销机制 |
| Authentication Context | 一次认证的保证上下文 | 记录 IAL/AAL/FAL、acr、amr、auth_time、风险和证据来源 |
| Consent | 用户授权与协议同意 | 记录用途、范围、版本、时间、来源和撤回状态 |
| Role / Permission / Policy | 授权模型 | 支持作用域、数据范围、条件和有效期 |
| Audit Event | 不可抵赖的操作记录 | 记录操作者、对象、动作、前后值、原因和结果 |
| Risk Event | 登录与账号风险事件 | 保存信号、风险评分、处置动作和证据 |
| Machine Principal | 非自然人主体 | 服务账号、工作负载、机器人、设备和代理主体，有负责人、用途和到期 |
| Client / API Resource | OAuth Client 与受保护资源 | 定义认证方式、回调、scope、audience、环境、负责人和生命周期 |
| Policy Decision | 一次授权决策 | 保存输入摘要、允许或拒绝、原因、义务、策略版本和有效期 |
| Security Signal | 持续安全状态信号 | 表达账号失陷、保证等级变化、会话撤销和风险升级 |
| Privacy Request | 数据主体权利请求 | 编排访问、更正、导出、限制、删除、撤回及跨系统证明 |
| Key / Certificate | 密钥与证书资产 | 记录用途、算法、负责人、版本、状态、到期、轮换和吊销 |

### 3.2 ID 体系

建议同时保留四层标识：

| 标识 | 用途 |
|---|---|
| Global User ID | 用户中心内部统一 UID，永久稳定 |
| Subject ID | 通过 OIDC 等协议向应用发布的用户标识，可配置为全局或应用级 |
| Membership ID | 用户在某业务线、租户或组织中的成员标识 |
| External ID | 旧系统、合作方、企业目录中的原始用户 ID |

设计要求：

- UID 不包含手机号、地区、注册时间、业务类型等可推断信息。
- 手机号和邮箱只是可变登录标识，不能作为主键。
- UID、Subject ID、Membership ID 均不得被重新分配给其他用户。
- 账号合并后保留旧 ID 到主账号的受控映射，防止历史数据失联。
- 对外部应用默认使用应用级 Subject，避免一个标识泄漏后串联全部业务数据。
- 只有确有跨业务关联需求的受信任服务，才允许获取 Global User ID。

### 3.3 数据权威边界

| 数据 | 权威系统 |
|---|---|
| 全局 UID、账号状态、登录身份、凭证 | 用户中心 |
| 公共头像、昵称、已验证手机号和邮箱 | 用户中心 |
| 用户在业务线中的启停状态 | 对应业务线或成员服务 |
| 订单、积分、等级、资产、内容数据 | 业务系统 |
| 企业员工、部门、岗位 | 企业目录或组织人事系统，用户中心同步映射 |
| 角色、权限和策略 | 统一授权中心或明确指定的业务权限域 |

### 3.4 全局状态机与不变量

关键对象必须使用正交状态机，禁止用一个账号状态同时表达生命周期、安全处置和业务状态：

- 用户生命周期：`PROVISIONAL → ACTIVE → DELETION_PENDING → ANONYMIZED`。
- 认证锁定状态：`ENABLED ⇄ LOCKED`；安全冻结状态独立为 `CLEAR ⇄ FROZEN`。解冻不得清除原有锁定。
- 成员状态：`INVITED → PENDING_APPROVAL → ACTIVE → SUSPENDED/BANNED/LEFT`。
- 认证器状态：`PENDING → ACTIVE → SUSPENDED/COMPROMISED → REVOKED/REPLACED`。
- 会话状态：`ACTIVE → EXPIRED/REVOKED`，失陷时先进入 `COMPROMISED` 并撤销。
- Grant 状态：`PENDING → ACTIVE → REVOKED/EXPIRED`。
- Consent 状态：`PENDING → GRANTED → WITHDRAWN/EXPIRED/SUPERSEDED`。
- 控制面配置：`DRAFT → VALIDATED → APPROVED → STAGED → ACTIVE → DEPRECATED/REVOKED`。

全局不变量：

1. UID、Subject ID、Membership ID 一经签发不得变更、复用或重新分配。
2. 同一唯一性作用域内，一个有效 Identifier 最多绑定一个有效 Global User。
3. 外部身份必须使用协议专用稳定键：OIDC 为 `issuer + sub`；SAML 为 IdP entityID、NameID、NameID Format 及适用的 SPNameQualifier；不得仅凭邮箱静默关联账号。
4. 全局安全状态、业务成员状态、租户状态必须正交建模并按最严格结果执行。
5. 冻结主体不得创建或刷新会话、登记认证器、完成找回或获得新授权。
6. Grant 或权限撤回后不得继续签发相关 Token；高风险权限不得使用过期缓存放行。
7. Client 禁用后不得签发新 Token，存量 Token 必须按安全等级在约定时限内失效。
8. 已匿名化主体不得恢复，旧 UID 只能作为受控墓碑或映射存在。
9. 高风险写入与领域事件必须通过本地事务和 Outbox 保证状态与事件不失配。
10. 相同幂等键和相同请求必须得到相同业务结果；相同幂等键的不同请求必须冲突。

### 3.5 身份、认证与联合保证等级

平台采用 IAL/AAL/FAL 或等价内部模型：

- **IAL**：自然人或组织身份核验程度，不得由“手机号已验证”推断为“自然人已实名”。
- **AAL**：本次认证强度，区分单因素、多因素、抗钓鱼和硬件保护。
- **FAL**：联合断言的保护程度，包括签名、加密、受众限制和持有者证明。

每个敏感操作必须声明最低保证等级、最大认证年龄、允许的认证器组合和风险上限。`acr` 表达达到的保证等级，`amr` 表达实际认证方式，`auth_time` 表达认证时间，三者不得混用。找回、认证器替换、客服处置和管理员恢复后必须重新计算保证等级。

## 4. 详细能力清单

优先级定义：

- **P0**：首期必须具备，否则无法安全、稳定地统一接入。
- **P1**：规模化运营或安全增强能力，建议第二阶段建设。
- **P2**：复杂组织、企业场景或精细化授权能力，按需要建设。

这里的优先级表示建设顺序，不表示目标态可永久缺失。P0/P1 属于通用目标态能力；P2 在对应场景启用后即成为该场景上线前的必备能力。

### 4.1 身份主体与账号生命周期

| 能力 | 子能力 | 优先级 |
|---|---|---|
| 全局用户主档 | UID 创建、查询、状态、来源、版本、创建时间 | P0 |
| 身份绑定 | 手机、邮箱、用户名、第三方账号、企业账号绑定 | P0 |
| 身份解绑与换绑 | 原身份验证、新身份验证、保护期、风险拦截 | P0 |
| 标识验证 | 手机、邮箱、外部身份可信状态与验证时间 | P0 |
| 重复账号治理 | 重复检测、人工处置、合并候选提示 | P1 |
| 账号合并 | 双账号强验证、冲突处理、历史 ID 映射、审计 | P1 |
| 账号拆分 | 误合并恢复、资产归属确认、审批和审计 | P2 |
| 游客身份 | 临时 UID、设备绑定、有效期、游客升级 | P1 |
| 服务账号 | 非自然人主体、密钥、负责人、到期和轮换 | P1 |
| 账号状态 | 待验证、正常、锁定、冻结、注销中、已注销 | P0 |
| 业务成员状态 | 未加入、正常、封禁、退出、待审批 | P0 |
| 注销流程 | 身份验证、影响提示、冷静期、撤回、执行 | P0 |
| 删除与匿名化 | PII 删除、业务通知、保留例外、不可逆确认 | P0 |
| 账号恢复 | 锁定恢复、注销撤回、身份重新验证 | P1 |
| 黑名单与限制 | 手机、邮箱、设备、IP、用户和外部身份限制 | P1 |

### 4.2 注册与登录认证

| 能力 | 子能力 | 优先级 |
|---|---|---|
| 注册策略 | 开放、邀请、白名单、地区、年龄、租户策略 | P0 |
| 手机注册登录 | 短信验证码、频控、号码规范化和验证 | P0 |
| 邮箱注册登录 | 邮件验证码、验证链接、域名策略 | P0 |
| 密码认证 | 设置、修改、强度、泄漏密码检查、现代自适应哈希 | P0 |
| 用户名登录 | 唯一性、保留词、大小写与规范化规则 | P1 |
| Magic Link | 邮件免密链接、单次使用、短有效期 | P2 |
| 第三方登录 | 微信、Apple、Google 等身份联合 | P1 |
| 企业 SSO | OIDC、SAML 等企业身份源接入 | P2 |
| Passkey | WebAuthn 注册、认证、凭证管理和跨设备使用 | P1 |
| 多因素认证 | TOTP、Passkey、安全密钥、恢复码 | P1 |
| 自适应认证 | 根据风险、设备和操作类型决定认证强度 | P1 |
| Step-up 二次认证 | 支付、改密、换绑、授权等敏感操作再次验证 | P0 |
| 账号找回 | 多证据验证、人工申诉、冷却期和风险审查 | P0 |
| 认证强度 | 记录本次认证方式、时间和可信等级 | P1 |
| 统一登录页 | 品牌配置、国际化、无障碍、业务参数化 | P0 |
| 原生端登录 | App、小程序安全跳转、深链和系统浏览器流程 | P0 |

### 4.3 会话、设备与令牌

| 能力 | 子能力 | 优先级 |
|---|---|---|
| 单点登录 | 多应用复用登录态、明确 SSO 域边界 | P0 |
| 单点退出 | 当前应用退出、全局退出、前后通道通知 | P0 |
| 会话生命周期 | 创建、续期、绝对过期、空闲过期、撤销 | P0 |
| 多设备管理 | 查看设备、设备命名、单设备退出、全设备退出 | P1 |
| 可信设备 | 可信设备标记、失效、重新验证 | P1 |
| Access Token | audience、scope、有效期、签名和最小声明 | P0 |
| Refresh Token | 轮换、重放检测、吊销和设备绑定 | P0 |
| Token 吊销 | 改密、冻结、风险事件后的会话失效 | P0 |
| Token 内省 | 适用于不便本地校验或需实时状态的应用 | P1 |
| 密钥发布 | JWKS、签名密钥版本和安全轮换 | P0 |
| 登录态同步 | 账号状态变化传播、缓存失效和版本校验 | P0 |
| 并发策略 | 最大设备数、最大会话数、互踢策略 | P1 |
| 服务间身份 | Client Credentials、工作负载身份、短期凭证 | P1 |
| Token 交换 | 受控服务委托和下游 audience 约束 | P2 |

### 4.4 用户资料与偏好

| 能力 | 子能力 | 优先级 |
|---|---|---|
| 公共资料 | 头像、昵称、语言、时区等通用字段 | P0 |
| 联系方式 | 手机、邮箱、验证状态、主联系方式 | P0 |
| 业务扩展资料 | 业务自定义字段、命名空间和访问边界 | P0 |
| 头像服务 | 上传、裁剪、格式转换、默认头像、内容安全 | P0 |
| 字段元数据 | 类型、必填、唯一、可见、可改、敏感等级 | P0 |
| 字段来源 | 用户填写、管理员、外部身份源、系统计算 | P1 |
| 资料变更历史 | 字段级时间、来源、操作者和前后值 | P1 |
| 资料可见性 | 用户、业务线、管理员、第三方的读取范围 | P0 |
| 偏好设置 | 语言、时区、主题、消息偏好 | P1 |
| 通知订阅 | 营销与事务通知分离、渠道和退订 | P1 |
| 实名与认证信息 | 按业务需要独立建设，严格隔离和最小化访问 | P2 |
| 数据质量 | 规范化、去重、有效性校验和完整度 | P1 |

### 4.5 业务线、应用、多租户与组织

| 能力 | 子能力 | 优先级 |
|---|---|---|
| 业务线管理 | 创建、状态、负责人、数据域和接入策略 | P0 |
| 应用管理 | Web、App、小程序、服务端等应用类型 | P0 |
| Client 管理 | Client ID、密钥、回调地址、作用域、环境 | P0 |
| 租户管理 | 租户创建、状态、套餐、配置和隔离 | P1 |
| 租户域名 | 域名验证、登录入口和身份源发现 | P2 |
| 组织结构 | 公司、部门、门店、项目组和层级 | P2 |
| 成员管理 | 加入、邀请、审批、转移、退出和封禁 | P1 |
| 用户组 | 静态组、动态组、组织组和权限绑定 | P2 |
| 委派管理员 | 租户管理员、业务管理员和有限管理范围 | P1 |
| 租户认证策略 | 密码、MFA、会话、企业 SSO 等租户策略 | P2 |
| 企业目录同步 | SCIM 或 API 同步用户、组织和状态 | P2 |
| 租户隔离 | 数据、缓存、日志、索引和权限全面隔离 | P1 |

### 4.6 权限与策略中心

权限能力采用分层设计：

~~~mermaid
flowchart LR
    U["用户 / 用户组 / 服务账号"] --> R["角色 Role"]
    R --> P["权限 Permission"]
    P --> RA["资源 Resource + 操作 Action"]
    RA --> DS["数据范围 Data Scope"]
    C["租户 / 业务线 / 环境 / 时间 / 风险条件"] --> D["策略决策"]
    DS --> D
    D --> E["允许 / 拒绝 + 原因 + 策略版本"]
~~~

| 能力 | 子能力 | 优先级 |
|---|---|---|
| 权限目录 | 标准化资源、操作、作用域和权限编码 | P0 |
| RBAC | 用户、用户组、角色、权限和绑定关系 | P0 |
| 作用域隔离 | 平台、业务线、应用、租户、组织层级 | P0 |
| 数据范围 | 全部、组织及下级、所在组织、指定集合、本人 | P0 |
| 默认拒绝 | 未明确授权时拒绝访问 | P0 |
| 显式拒绝 | 高优先级拒绝规则 | P1 |
| 权限继承 | 角色、组织、资源层级继承和冲突规则 | P1 |
| 临时授权 | 有效期、申请理由、到期自动回收 | P1 |
| 委派授权 | 在有限边界内转授权限 | P2 |
| ABAC | 按用户、资源、环境、风险等属性判断 | P2 |
| 关系型授权 | 所有者、成员、协作者、上下级等关系 | P2 |
| 职责分离 | 互斥角色、申请与审批人隔离 | P1 |
| 策略决策服务 | 集中 PDP，输出允许、拒绝、原因和版本 | P1 |
| 策略执行点 | 网关、后端服务和前端的统一执行约定 | P1 |
| 策略管理 | 创建、测试、审批、发布、灰度、回滚 | P1 |
| 权限模拟 | 上线前模拟用户对资源的实际权限 | P1 |
| 权限解释 | 回答为什么允许或拒绝 | P1 |
| 权限审计 | 谁在何时拥有、授予、使用过何种权限 | P0 |
| 权限复核 | 定期复核、闲置权限识别和自动回收 | P2 |

实施建议：

- 首期落地“RBAC + 业务线/租户作用域 + 数据范围”。
- Token 中只携带稳定、粗粒度声明，避免塞入全部细粒度权限。
- 支付、退款、数据导出、管理员操作等高风险权限应实时决策。
- 普通低风险读操作可以使用短期缓存，但必须带策略版本和失效机制。
- 前端权限只用于界面展示，后端必须再次进行授权校验。

### 4.7 身份联合与企业接入

| 能力 | 子能力 | 优先级 |
|---|---|---|
| 社交身份联合 | 外部账号绑定、解绑、冲突和状态同步 | P1 |
| 上游 OIDC | 对接第三方或企业身份提供方 | P2 |
| SAML 联合 | 面向存量企业身份系统 | P2 |
| 身份源路由 | 按邮箱域、租户、入口选择身份源 | P2 |
| Just-in-Time 创建 | 首次企业登录时创建用户和成员关系 | P2 |
| 目录同步 | 通过 SCIM/API 创建、更新、停用企业用户 | P2 |
| 属性映射 | 外部属性到公共资料、组织和角色的映射 | P2 |
| 离职停用 | 外部用户停用后撤销会话和业务权限 | P2 |
| 账号链接 | 企业身份与已有全局用户的安全关联 | P2 |

### 4.8 安全与风险控制

| 能力 | 子能力 | 优先级 |
|---|---|---|
| 登录防护 | 防暴力破解、撞库、用户名枚举和凭证填充 | P0 |
| 验证码防护 | 发送频控、图形验证、设备限制、防短信轰炸 | P0 |
| 设备风险 | 新设备、模拟器、越狱、Root、设备指纹异常 | P1 |
| 网络风险 | 代理、匿名网络、异常 IP、异地和不可能旅行 | P1 |
| 行为风险 | 登录频率、操作序列和异常行为检测 | P1 |
| 风险评分 | 聚合信号并输出低、中、高风险等级 | P1 |
| 风险处置 | 放行、验证码、MFA、拒绝、冻结、人工审核 | P1 |
| 敏感操作保护 | 改密、换绑、导出、授权、注销二次确认 | P0 |
| 安全通知 | 新设备、改密、换绑、MFA 变更和高风险登录通知 | P1 |
| 管理员安全 | 强制 MFA、IP 限制、审批、操作水印和会话限制 | P0 |
| 凭证安全 | 凭证隔离、强哈希、密钥加密和访问最小化 | P0 |
| 密钥管理 | KMS/HSM、分环境密钥、轮换、吊销和使用审计 | P0 |
| 数据加密 | 传输加密、静态加密、字段级加密和脱敏 | P0 |
| 应急处置 | 全局登出、密钥紧急轮换、应用禁用和风险规则开关 | P0 |
| 安全运营 | 告警、调查、证据、处置工单和复盘 | P1 |

### 4.9 隐私、协议与数据治理

| 能力 | 子能力 | 优先级 |
|---|---|---|
| 用户协议 | 版本、适用地区、接受时间、来源和证据 | P0 |
| 隐私同意 | 用途、数据项、第三方、有效期和撤回 | P0 |
| 授权范围 | 应用请求 scope、用户确认和最小授权 | P0 |
| 数据分类分级 | 公开、内部、敏感、严格敏感等分级 | P0 |
| 数据最小化 | 只收集和下发业务必需字段 | P0 |
| 访问与更正 | 用户查看、修改和申诉纠错 | P1 |
| 数据导出 | 身份验证、异步生成、加密下载和过期销毁 | P1 |
| 删除与注销 | 跨业务通知、执行状态、例外和结果证明 | P0 |
| 数据保留 | 按数据类型、用途、地区配置保留周期 | P1 |
| 匿名化 | 删除直接标识，评估重新识别风险 | P1 |
| 跨境与地域 | 数据驻留、跨境传输和地区路由策略 | P2 |
| 未成年人保护 | 年龄识别、监护人同意和特殊限制 | P2 |
| 隐私审计 | 谁因何目的访问过哪些敏感信息 | P0 |

具体合规要求需由法务、安全和隐私负责人根据业务运营地区确认，不能仅由技术规则代替法律判断。

### 4.10 开放平台与开发者体验

| 能力 | 子能力 | 优先级 |
|---|---|---|
| 标准认证端点 | authorize、token、userinfo、logout、revoke 等 | P0 |
| 用户管理 API | 用户、身份、资料、状态、成员关系管理 | P0 |
| 权限 API | 角色、权限、策略、授权关系和决策 | P0/P1 |
| SDK | Web、服务端、iOS、Android、小程序 | P0 |
| 统一登录组件 | 托管登录页、可配置主题和品牌 | P0 |
| Webhook | 业务订阅指定用户事件 | P0 |
| 事件总线 | 高吞吐内部事件分发、重试和回放 | P0 |
| 应用自助台 | 应用创建、密钥、回调地址、scope 和文档 | P1 |
| 沙箱环境 | 测试用户、模拟身份源、测试密钥和事件重放 | P1 |
| API 版本管理 | 兼容窗口、弃用通知和迁移指南 | P0 |
| 幂等与重试 | 幂等键、事件 ID、去重和重试退避 | P0 |
| 限流配额 | 应用、租户、IP、接口维度限流 | P0 |
| 变更通知 | API、字段、协议和密钥策略变更通知 | P1 |
| 接入质量 | 合规检查、回调校验、安全扫描和上线审批 | P1 |

建议的核心事件：

- user.created
- user.status.changed
- user.anonymized
- identity.bound
- identity.unbound
- identifier.verified
- profile.updated
- membership.created
- membership.status.changed
- session.revoked
- credential.changed
- consent.granted
- consent.revoked
- authorization.changed
- risk.detected

所有事件至少包含事件 ID、事件类型、主体 ID、业务作用域、发生时间、数据版本、追踪 ID 和最小必要载荷。

### 4.11 管理、客服与运营

| 能力 | 子能力 | 优先级 |
|---|---|---|
| 用户搜索 | UID、Subject、手机号、邮箱、外部 ID 等受控查询 | P0 |
| 用户全景 | 身份、状态、成员、会话、风险和操作时间线 | P0 |
| 状态处置 | 锁定、冻结、解冻、业务封禁和注销处理 | P0 |
| 会话处置 | 单设备退出、全设备退出、Token 吊销 | P0 |
| 凭证处置 | 重置 MFA、恢复码、安全找回流程 | P1 |
| 合并拆分工单 | 证据收集、审批、执行和回滚计划 | P1 |
| 客服权限 | 字段脱敏、按业务授权、临时提权和到期回收 | P0 |
| 高风险审批 | 双人复核、职责分离、理由和附件 | P1 |
| 操作审计 | 操作者、来源、对象、前后值、理由和结果 | P0 |
| Break-glass | 紧急管理员账号、强审计、告警和使用后复核 | P1 |
| 报表指标 | 注册、登录、留存、失败率、风险和渠道质量 | P1 |
| 工单集成 | 与客服、安全、法务和隐私工单系统联动 | P2 |

不建议提供无审批、无审计的“直接登录为用户”能力。确有排障需要时，应采用受控代理、限定范围、明确提示和全程审计。

### 4.12 审计与可观测性

| 能力 | 子能力 | 优先级 |
|---|---|---|
| 认证审计 | 注册、登录成功/失败、MFA、找回和退出 | P0 |
| 管理审计 | 用户、应用、密钥、角色、策略和配置变更 | P0 |
| 数据访问审计 | 敏感资料查询、导出、解密和批量操作 | P0 |
| 审计完整性 | 防篡改、独立存储、访问控制和保留周期 | P0 |
| 链路追踪 | 请求 ID、事件 ID、会话 ID 和跨服务追踪 | P0 |
| 技术指标 | QPS、延迟、错误率、资源和依赖健康度 | P0 |
| 业务指标 | 注册成功率、登录成功率、找回成功率 | P0 |
| 安全指标 | 撞库、验证码滥用、异常登录和风险处置 | P0 |
| 应用指标 | 各业务线成功率、回调错误和 Token 错误 | P0 |
| 告警管理 | 分级、降噪、值班、升级和恢复通知 | P0 |

### 4.13 高可用、容灾与平台工程

| 能力 | 子能力 | 优先级 |
|---|---|---|
| 多实例高可用 | 服务无状态化、负载均衡和故障隔离 | P0 |
| 数据库高可用 | 主从、自动切换、备份和恢复验证 | P0 |
| 缓存降级 | 缓存穿透保护、热点隔离和回源控制 | P0 |
| 限流熔断 | 按应用、租户、接口和依赖实施 | P0 |
| 容灾 | 明确 RTO/RPO、跨可用区和灾备演练 | P0 |
| 容量管理 | 用户量、峰值登录、Token 签发和事件积压预测 | P0 |
| 灰度发布 | 按应用、租户、用户比例灰度和快速回滚 | P0 |
| 配置治理 | 分环境、审批、版本、校验和回滚 | P0 |
| 数据一致性 | 凭证和封禁强一致，资料同步最终一致 | P0 |
| 事件可靠性 | Outbox、至少一次投递、幂等、重放和对账 | P0 |
| 故障降级 | 已签发 Token 尽量可本地验证，敏感授权失败关闭 | P0 |
| 演练 | 容灾、密钥泄漏、短信故障、数据库故障等演练 | P1 |

建议明确以下服务目标，而不是仅写“高可用”：

- 月度可用性目标。
- 登录和 Token 签发的 P95/P99 延迟。
- 峰值注册、登录和验证请求容量。
- 故障恢复时间 RTO 和数据恢复点 RPO。
- 账号冻结到全部会话失效的最大时间。
- 用户资料变更到业务系统可见的最大延迟。
- 安全事件告警和人工响应时间。

### 4.14 OAuth、OIDC 与高安全协议 Profile

平台不得只声明“支持 OAuth/OIDC”，必须按客户端和风险类型提供版本化安全 Profile。

| 能力 | 子能力 | 优先级 |
|---|---|---|
| 客户端安全 Profile | 机密 Web、SPA/BFF、原生端、小程序、设备、机器客户端分型 | P0 |
| 安全授权流程 | Authorization Code + PKCE S256，禁用 Implicit 与 ROPC | P0 |
| 回调安全 | 精确匹配、分环境注册、禁止生产通配符和开放重定向；仅原生端 loopback 动态端口按 RFC 8252 例外 | P0 |
| 请求绑定 | state、nonce、PKCE、Client、redirect URI 和授权码绑定 | P0 |
| Token 校验 Profile | issuer、audience、azp、exp、iat、nbf、算法和类型校验 | P0 |
| Token 类型隔离 | ID Token、Access Token、Refresh Token 用途和受众严格分离 | P0 |
| Refresh Token 安全 | 家族轮换、重放检测、设备绑定和家族级吊销 | P0 |
| Client 认证 | private_key_jwt、mTLS、密钥轮换；公开客户端禁止持有 secret | P0 |
| 元数据与密钥发布 | Discovery、Authorization Server Metadata、JWKS 双钥窗口 | P0 |
| 原生端安全 | 系统浏览器、Universal/App Link、claimed HTTPS、loopback 规则 | P0 |
| 完整退出 | RP-Initiated、Front-Channel、Back-Channel Logout | P0 |
| 资源约束 | Resource Indicators、明确 audience 和资源服务器注册 | P1 |
| 高安全授权请求 | PAR、JAR/JARM、RAR | P2 |
| 发送方约束 Token | DPoP 或 mTLS sender-constrained token | P2 |
| FAPI 2.0 Profile | 面向资金、核心资产和高价值数据应用 | P2 |

高安全能力为场景条件能力：普通低风险应用可以使用基础 Profile；一旦涉及资金、批量敏感数据或强监管，PAR、发送方约束 Token 和 FAPI 级约束即成为上线前必备项。

### 4.15 认证器生命周期、账号恢复与保证等级

| 能力 | 子能力 | 优先级 |
|---|---|---|
| 认证器登记 | 近期认证、风险检查、用户确认、设备和来源记录 | P0 |
| 认证器状态 | 待激活、有效、暂停、锁定、失陷、撤销、替换、过期 | P0 |
| 认证器替换与挂失 | 新旧认证器交接、保护期、会话处置和安全通知 | P0 |
| 密码生命周期 | 泄漏口令拦截、参数升级、重置、失陷和历史策略 | P0 |
| 恢复码 | 一次展示、哈希存储、单次使用、批次轮换和审计 | P1 |
| Passkey 元数据 | AAGUID、UV、可发现性、同步/设备绑定、备份状态 | P1 |
| 认证器可信策略 | Attestation、允许型号、抗钓鱼和硬件保护等级 | P2 |
| 保证等级模型 | IAL/AAL/FAL、acr/amr/auth_time 映射 | P0 |
| 敏感操作要求 | 最低等级、最大认证年龄、风险上限和 Step-up | P0 |
| 高保证账号恢复 | 多证据、双人复核、等待期、通知和申诉 | P0 |
| 恢复后处置 | 旧认证器失效、相关会话撤销、保证等级重算 | P0 |
| 管理员恢复 | 独立恢复通道、抗钓鱼认证和强审计 | P0 |

恢复流程不得比正常认证更容易。删除最后一个强认证器、重置管理员 MFA、修改恢复渠道和完成账号合并必须视为高风险操作。

### 4.16 标识符治理、账号链接与回收防护

| 能力 | 子能力 | 优先级 |
|---|---|---|
| 标识规范化 | E.164、邮箱国际化与大小写、Unicode 用户名和保留字 | P0 |
| 唯一性作用域 | 全局、租户、应用级规则及迁移方式 | P0 |
| 外部身份主键 | OIDC 使用 issuer + sub；SAML 使用 entityID + NameID 语义；禁止仅按邮箱静默链接 | P0 |
| 解绑与隔离期 | 历史归属、冷却期、墓碑和重新绑定规则 | P0 |
| 回收号码防护 | 运营商号码回收、企业邮箱重分配、SIM swap 风险 | P0 |
| 域名所有权变化 | 企业域名持续验证、失效和租户接管保护 | P1 |
| 账号链接证据 | 双账号验证、来源可信度、冲突和人工处置 | P0 |
| 标识版本 | 事件和下游副本携带 UID 与标识版本 | P0 |
| 历史隐私保护 | 历史标识受控墓碑、不可逆摘要和访问审计 | P0 |

新持有人完成手机号或邮箱验证，不得自动继承旧账号、历史授权、Consent、Membership 或恢复能力。

### 4.17 机器身份与工作负载身份

| 能力 | 子能力 | 优先级 |
|---|---|---|
| 机器主体分类 | Client、服务账号、工作负载、机器人、Agent、设备和 CI/CD | P0 |
| 负责人和用途 | Owner、环境、代码或工作负载来源、用途、到期 | P0 |
| 短期凭证 | 工作负载联合、mTLS、证书或短期 Token，减少静态密钥 | P0 |
| 工作负载证明 | 云身份联合、工作负载身份、证书自动签发与轮换 | P1 |
| 服务间授权 | audience、scope、租户和资源最小化 | P0 |
| 委托链 | Token Exchange、原始主体、代理主体和委托深度 | P1 |
| 代表用户调用 | Actor 与 Subject 分离，禁止无边界 impersonation | P0 |
| 机器权限治理 | 人机角色分离、定期复核、闲置与孤儿检测 | P0 |
| 凭证失陷响应 | 紧急吊销、影响分析、轮换和存量 Token 处置 | P0 |
| 自动到期 | 负责人离职、工作负载下线、长期未使用时自动回收 | P1 |

服务间调用是平台自身运行的基础，因此机器身份治理属于生产 P0，而不是上线后的附加能力。

### 4.18 授权执行、控制面与配置治理

| 能力 | 子能力 | 优先级 |
|---|---|---|
| PAP | 策略创建、校验、审批、版本、发布和回滚 | P0 |
| PDP | 输入标准化、允许/拒绝、原因、义务、版本和有效期 | P0 |
| PEP | 网关、服务、数据层和异步消费者执行约定 | P0 |
| PIP | 属性来源、可信度、新鲜度和失败语义 | P0 |
| 决策上下文 | Subject、Actor、Resource、Action、Tenant、Risk、Assurance | P0 |
| 冲突规则 | deny-overrides、继承、显式拒绝和缺失属性 | P0 |
| 决策缓存 | 完整缓存键、策略版本、安全 epoch 和主动失效 | P0 |
| TOCTOU 防护 | 高风险写入在提交点重决策或绑定资源版本 | P0 |
| 列表授权过滤 | 查询阶段按数据范围过滤，禁止只校验详情接口 | P0 |
| 控制面状态机 | 草稿、校验、审批、灰度、激活、弃用、撤销 | P0 |
| 双人复核 | 密钥、回调、身份源、高权限和保留规则变更 | P0 |
| 配置漂移检测 | 环境晋级、运行版本、差异检测和自动回滚 | P1 |
| 安全引导 | 根密钥、首个管理员、租户所有权和灾备恢复信任链 | P0 |
| 紧急控制 | Client 禁用、密钥吊销、身份源隔离、强制 MFA | P0 |

前端不是可信 PEP。高风险授权在 PDP、PIP 或撤销状态不可确定时必须失败关闭。

### 4.19 事件安全、持续安全信号与隐私编排

| 能力 | 子能力 | 优先级 |
|---|---|---|
| 统一事件信封 | schema、producer、subject、tenant、版本、因果和敏感级别 | P0 |
| 发布者授权 | Producer 身份认证和事件类型发布权限 | P0 |
| Webhook 安全 | 签名、时间戳、防重放、接收方认证和密钥双轮换 | P0 |
| 出站安全 | 目标域名验证、allowlist、SSRF 防护和租户隔离 | P0 |
| Schema 治理 | 兼容检查、弃用窗口和破坏性版本升级 | P0 |
| 顺序与恢复 | 单主体顺序键、死信、补拉、受控重放和对账 | P0 |
| 持续安全信号 | 账号失陷、保证等级变化、会话撤销和风险升级 | P1 |
| 数据目录 | 字段用途、合法依据、来源、接收方、地域和保留期 | P0 |
| 隐私请求编排 | 访问、更正、导出、限制、删除、反对和撤回 | P0 |
| 下游处理证明 | 跨系统发现、分派、重试、阻断、期限和完成证明 | P0 |
| 法律保留 | Legal Hold、反欺诈保留和删除请求冲突决策 | P0 |
| 全介质删除 | 备份、日志、事件、索引、分析和训练数据策略 | P1 |
| Consent 传播 | 撤回同步到 Grant、Token Claims、Webhook 和数据副本 | P0 |

Consent、营销订阅、协议接受和 OAuth Grant 必须分别建模，不能用一次勾选替代所有合法处理依据。

### 4.20 全生命周期欺诈与协议一致性

| 能力 | 子能力 | 优先级 |
|---|---|---|
| 注册与养号风险 | 机器人、批量注册、合成身份、邀请和优惠滥用 | P0 |
| 认证风险 | 撞库、MFA fatigue、SIM swap、邮箱接管和会话窃取 | P0 |
| 恢复与客服风险 | 找回欺诈、社工、内部滥用和证据复核 | P0 |
| 变更风险 | 绑定、换绑、合并、拆分、授权和注销撤回 | P0 |
| 机器凭证风险 | 孤儿身份、异常调用、凭证泄露和委托滥用 | P0 |
| 风险案件闭环 | 证据、审核、申诉、反馈标签和复盘 | P1 |
| 风险模型治理 | 版本、特征来源、漂移、误伤、公平性和解释 | P1 |
| OIDC/OAuth 一致性 | Provider、RP、Logout 和安全 Profile 测试 | P0 |
| WebAuthn/SCIM/SAML 互操作 | 标准正向、负向和跨版本测试 | P1 |
| 协议攻击测试 | mix-up、code replay、Token substitution、算法降级 | P0 |
| 密钥轮换测试 | 双钥窗口、旧 Token、JWKS 缓存和时钟偏移 | P0 |
| API/Event 契约测试 | Schema 兼容、幂等、乱序、重放和错误语义 | P0 |
| 状态机模型测试 | 属性测试、不变量检查和并发竞争 | P0 |
| 应用上线认证 | 每个新 Client 自动执行适用 Profile 检查 | P0 |

风险指标必须同时覆盖账号接管率、欺诈损失、恢复欺诈率、误拒率和申诉改判率，不能只统计拦截量。

## 5. 关键业务流程

### 5.1 新用户注册

~~~mermaid
sequenceDiagram
    participant U as 用户
    participant A as 业务应用
    participant I as 身份平台
    participant R as 风控
    participant E as 事件总线

    U->>A: 发起注册
    A->>I: 跳转统一注册页
    I->>R: 评估设备、IP、行为风险
    R-->>I: 风险等级与验证要求
    I->>U: 验证手机号/邮箱及必要的 MFA
    I->>I: 创建 Global User、Identity、Membership
    I-->>A: 返回授权码
    A->>I: 换取 Token
    I->>E: 发布用户和成员创建事件
    A-->>U: 完成登录
~~~

### 5.2 账号合并

1. 用户明确发起合并，而不是系统静默合并。
2. 对两个账号分别完成强身份验证。
3. 检查账号状态、风险、实名冲突、租户冲突和资产冲突。
4. 各业务系统返回资产处理策略或阻断原因。
5. 展示不可逆影响并由用户再次确认。
6. 确定主账号，迁移登录身份和成员关系。
7. 旧 UID 保留别名映射，不允许复用。
8. 吊销双方全部会话并要求重新登录。
9. 发布合并事件，执行下游对账。
10. 保存完整审计记录和有限期恢复方案。

### 5.3 修改手机号

1. 验证当前登录态和原手机号；无法使用时进入增强找回流程。
2. 验证新手机号并检查占用、黑名单和风险。
3. 对高风险场景要求 MFA、等待期或人工审核。
4. 修改后吊销高风险会话，通知原、新手机号。
5. 记录历史，但普通管理员不得直接查看完整旧号码。

### 5.4 冻结与封禁

- **全局安全冻结**：用户中心执行，阻止所有业务新登录并吊销会话。
- **业务封禁**：仅修改对应 Membership，不影响其他业务。
- **租户停用**：阻止用户进入指定租户，不影响个人账号或其他租户。
- **权限回收**：可独立于账号状态执行，适用于员工转岗、离职等场景。

### 5.5 注销与删除

1. 强身份验证和风险检查。
2. 汇总受影响业务、权益、余额、合同和未完成事项。
3. 业务系统返回可注销或阻断结果。
4. 用户确认后进入注销中或冷静期。
5. 冷静期内限制新增关键业务，并允许撤销。
6. 执行全局登出、身份解绑、PII 删除或匿名化。
7. 通知各业务系统执行其数据策略。
8. 对账并形成可审计的处理结果。

### 5.6 认证器登记、替换与高保证恢复

1. 校验当前会话保证等级、认证新鲜度和风险。
2. 对新认证器完成持有证明、用户验证和必要的可信度检查。
3. 原子创建认证器并递增用户 security epoch。
4. 替换场景将旧认证器标记为 `REPLACED`，不得直接删除审计证据。
5. 撤销相关高风险会话和 Refresh Token family。
6. 向原渠道和新渠道发送安全通知，并在保护期内限制敏感操作。
7. 无可用认证器时进入高保证恢复：多证据、等待期、人工复核和双人审批。
8. 恢复后重新计算 AAL，禁止自动恢复到高保证状态。

### 5.7 完整全局退出

1. 确定退出作用域：当前应用、当前设备、租户、全部设备或全局。
2. 撤销目标 OP Session、设备 Session 和 Refresh Token family。
3. RP-Initiated Logout 只允许已注册的 post-logout URI，校验请求上下文并原样返回 state。
4. Back-Channel Logout 向 RP 发送签名 Logout Token，RP 校验 `iss + aud + sid/sub + iat + jti + events` 并防重放。
5. Front-Channel Logout 通过已注册 URI 传递 `iss + sid`，不使用 Logout Token，RP 校验发行方和本地会话。
6. 对不可达 RP 重试并进入死信，平台展示“部分退出”状态。
7. 对短期 Access Token 使用撤销表、安全 epoch 或等待自然过期，并满足最大残留 SLA。
8. 完成对账并记录每个 RP 的退出结果。

### 5.8 企业离职与持续撤权

1. 企业目录通过 SCIM/API 将人员状态置为停用。
2. 用户中心验证来源和版本，停用对应企业 Identity 与 Membership。
3. 撤销该租户内角色、Grant、Session 和委托权限。
4. 通知应用清理本地会话和缓存，不影响个人账号及其他租户。
5. 超过撤权 SLA 未完成时触发安全告警和人工处置。
6. 目录恢复人员时不得自动恢复历史高权限，必须重新评估授权。

### 5.9 Client 或机器凭证失陷

1. 将 Client、证书或机器凭证标记为 `COMPROMISED` 并阻止新签发。
2. 确定受影响 audience、scope、租户、Token、Webhook 和委托链。
3. 吊销相关 Token family，轮换密钥并保持安全的双钥或零信任切换窗口。
4. 通知资源服务器和订阅方执行紧急拒绝。
5. 对异常调用、数据访问和权限使用进行回溯调查。
6. 恢复前必须更换凭证、修复根因并重新通过接入 Profile。

### 5.10 Consent 撤回与隐私请求

1. 强认证用户并确认请求范围、合法依据和数据主体。
2. 原子更新 Consent/Privacy Request 状态并停止新的相关 Grant。
3. 编排下游系统停止处理、撤销订阅、删除或导出数据。
4. 法律保留或业务阻断必须返回明确原因、范围和预计处理方式。
5. 失败任务按幂等语义重试，不得因部分失败宣布完成。
6. 汇总下游证明、审计、例外和完成时间，形成用户可获取的结果。

### 5.11 标识回收后的重新验证

1. 新持有人验证手机号或邮箱时查询受控历史墓碑和隔离期。
2. 结合解绑时间、设备、SIM swap、历史主体和行为风险评估。
3. 不得凭新验证结果自动登录、恢复或链接旧账号。
4. 需要关联时必须对旧账号和新标识分别提供足够证据。
5. 关联失败时创建新主体或进入人工处置，不泄漏旧主体存在性。

## 6. 系统交互边界

~~~mermaid
flowchart TB
    C["Web / App / 小程序 / 企业门户"] --> G["认证入口 / API 网关"]
    G --> AUTH["认证与联合登录"]
    G --> USER["用户、身份与资料"]
    G --> SESSION["会话与 Token"]
    G --> AUTHZ["权限策略与决策"]
    AUTH --> RISK["风险与 MFA"]
    USER --> CONSENT["隐私、协议与同意"]
    AUTH --> AUDIT["审计与安全事件"]
    SESSION --> AUDIT
    AUTHZ --> AUDIT
    USER --> EVENT["事件总线 / Webhook"]
    EVENT --> BIZ["各业务系统"]
    ADMIN["平台、业务、租户管理后台"] --> G
~~~

集成约束：

- 业务应用只通过标准协议和 API 接入。
- 禁止业务直接写用户中心数据表。
- 业务本地保存的用户资料副本必须明确用途、字段和同步机制。
- 事件消费必须幂等，并支持根据数据版本拒绝旧事件覆盖新数据。
- 用户中心不在 Token 中放入不必要的 PII。
- 业务后端校验 issuer、audience、有效期、签名和授权范围。
- 所有人类用户客户端使用授权码模式和 PKCE S256，不使用 Implicit 或 ROPC。
- 回调地址必须精确匹配；浏览器、移动端和小程序不得保存客户端密钥。
- ID Token 只用于客户端确认认证结果，不得作为 API Access Token 使用。
- 业务必须区分 Subject 与 Actor，代理服务代表用户调用时保留完整委托链。
- Webhook 必须验签、校验时间戳并拒绝重放；事件消费者必须支持权威快照补拉。
- 高风险授权、冻结、撤销和保证等级变化必须通过近实时控制通道传播。

## 7. 数据一致性与缓存策略

| 数据类型 | 一致性建议 | 原因 |
|---|---|---|
| 密码、MFA、身份绑定 | 强一致 | 防止旧凭证继续生效 |
| 全局冻结、Token 吊销 | 接近实时 | 属于安全控制面 |
| 角色和高风险权限 | 实时或短缓存 | 防止权限变更延迟 |
| 普通资料 | 最终一致 | 可通过事件同步提高可用性 |
| 业务成员关系 | 权威域写入、事件同步 | 避免跨域双写 |
| 审计日志 | 可靠追加 | 不允许业务更新覆盖 |
| 标识绑定与认证器 | 强一致 + 唯一约束 | 防止并发绑定和恢复竞态 |
| Grant 与高风险权限 | 强一致或有界陈旧 | 撤回后不得继续签发或放行 |
| 控制面配置 | 版本化发布 | 未审批版本不得在线生效 |
| 隐私请求 | Saga + 对账 | 跨系统处理允许部分进度但不得虚假完成 |

下游系统需具备：

- 基于事件 ID 的幂等消费。
- 基于数据版本的顺序保护。
- 失败重试和死信处理。
- 定期全量或增量对账。
- 事件丢失后的主动补拉能力。
- 基于 security epoch、policy version 和 revocation watermark 的缓存失效。
- 对跨领域流程展示处理中、部分失败、已补偿和需人工处置状态。

## 8. 管理角色建议

| 角色 | 管理范围 |
|---|---|
| 平台安全管理员 | 安全策略、风险规则、全局冻结和安全事件 |
| 平台应用管理员 | 业务线、应用、Client、回调地址和协议配置 |
| 平台用户管理员 | 用户生命周期处置，不默认拥有敏感信息明文权限 |
| 平台审计员 | 只读查看审计，不可修改用户或策略 |
| 业务线管理员 | 仅管理本业务成员、业务角色和业务配置 |
| 租户管理员 | 仅管理本租户成员、组织和租户策略 |
| 客服人员 | 脱敏查询和有限处置，敏感操作需审批 |
| 隐私管理员 | 数据请求、授权记录、删除和保留策略 |
| 紧急管理员 | 仅在应急场景使用，强制告警和事后复核 |

高权限账号不得多人共享，必须使用独立身份、MFA、短会话和完整审计。

## 9. 核心非功能要求

### 9.1 安全

- 凭证与普通用户资料分库或至少分安全域存储。
- 密码采用现代自适应哈希算法，参数可升级。
- TOTP 密钥、第三方令牌和客户端密钥应加密存储。
- 登录接口统一实施限流、枚举防护和错误信息收敛。
- 管理后台必须强制 MFA，并限制高风险操作。
- 日志、追踪、指标和事件中不得记录密码、验证码、完整 Token。
- 对手机号、邮箱、证件号等信息按角色脱敏。
- 所有客户端和服务端密钥均具备负责人、用途、有效期和轮换记录。

### 9.2 可用性

- 认证链路避免依赖非核心业务服务。
- 已签发 Token 可在短期内由业务本地校验，降低认证中心故障影响。
- 冻结、吊销和高风险授权需有实时或近实时通道。
- 短信、邮件、第三方身份源故障时提供替代认证或明确降级策略。
- 不同业务线和租户之间进行配额、资源和故障隔离。

### 9.3 性能与容量

- 按日活用户、并发登录、峰值倍数、验证码量、Token 签发量估算容量。
- 单独评估大促、开学、发薪、直播和批量企业登录等瞬时峰值。
- 对用户检索、审计检索和在线认证采用不同存储及性能策略。
- 避免把完整权限列表和大量资料放入 Token。

### 9.4 可维护性

- 领域 API 和事件契约版本化。
- 业务规则、认证策略、风险规则和租户策略可配置但必须可审计。
- 所有策略变更支持校验、审批、灰度和回滚。
- 管理后台避免直接操作底层数据。

## 10. 旧系统迁移能力

迁移不是一次性脚本，而是一组长期能力：

| 能力 | 说明 |
|---|---|
| 旧 ID 映射 | 保存旧系统用户 ID 与 Global User ID 的映射 |
| 数据清洗 | 手机、邮箱规范化，异常值和重复值识别 |
| 重复识别 | 形成候选关系，不直接自动合并 |
| 密码迁移 | 支持合规的渐进式密码升级或用户重置 |
| 双轨运行 | 迁移期间支持新旧认证系统有限期并行 |
| 灰度切换 | 按业务线、租户、用户比例逐步迁移 |
| 差异对账 | 比对用户、身份、状态和资料差异 |
| 回滚方案 | 保留切换点、映射和回退路径 |
| 用户沟通 | 需要改密、重新绑定或重新授权时明确通知 |
| 下游改造 | 禁止继续以手机号或旧业务 ID 作为跨系统主键 |

推荐迁移顺序：

1. 盘点所有业务用户表、登录方式、ID、资料、状态和权限。
2. 确定 Global User ID 和旧 ID 映射规则。
3. 先接统一认证协议，再迁移公共资料。
4. 建立重复账号处置流程。
5. 小业务或内部用户先灰度。
6. 逐步迁移核心业务并持续对账。
7. 停止旧系统新增账号。
8. 完成存量迁移后下线旧认证能力。

## 11. 分期建设路线

### 阶段 0：规则与边界设计

- 定义什么是同一个用户。
- 定义 Global User ID、Subject ID 和 Membership ID。
- 明确公共资料和业务私有资料。
- 明确全局状态和业务状态。
- 明确 B2C、B2B、多租户和组织场景。
- 盘点现有账号、身份源、权限和迁移风险。
- 确认安全、隐私、可用性和容量目标。
- 定义所有关键对象的状态机、全局不变量、唯一性和并发语义。
- 定义 IAL/AAL/FAL 或等价保证等级及敏感操作要求。
- 定义 OAuth/OIDC 客户端安全 Profile、Token Claims 和退出契约。
- 定义控制面信任链、机器身份边界和端到端撤销 SLA。
- 建立威胁模型、协议负向测试和自动化验收追踪规则。

### 阶段 1：统一身份最小闭环

- Global User、Identity、Credential、Application、Membership。
- 手机、邮箱、密码、验证码注册登录。
- 统一登录页和符合安全 Profile 的 OIDC/OAuth 2.0 接入。
- 公共用户资料、头像、身份绑定和换绑。
- 基础 MFA、Step-up、认证器生命周期和安全找回。
- 会话、Grant、Token family、完整退出和吊销。
- 全局冻结、业务封禁、注销。
- Client/API Resource、机器身份、API、基础 SDK 和安全事件通知。
- 基础 RBAC、业务作用域和数据范围。
- 管理后台、控制面审批、审计、限流、监控和高可用。
- Webhook 验签、防重放、补拉、对账和协议一致性门禁。

### 阶段 2：安全和规模化运营

- Passkey、认证器可信策略、第三方登录和自适应认证。
- 设备管理、登录通知和风险控制。
- 高保证人工恢复、账号合并和游客升级。
- 业务线自助接入和沙箱。
- 权限决策服务、临时授权和权限解释。
- 隐私同意、数据导出、删除和保留策略。
- 旧系统迁移平台、对账和事件回放。
- 持续安全信号、全生命周期欺诈和风险案件闭环。

### 阶段 3：企业、多租户和精细授权

- 企业 SSO、SAML、SCIM。
- 租户、组织、部门、用户组和委派管理员。
- ABAC、关系型权限、职责分离和定期复核。
- 身份源路由、属性映射和离职自动停用。
- 地域化部署、数据驻留和复杂合规策略。
- 高价值场景的 FAPI 2.0、PAR、RAR、DPoP 或 mTLS Profile。

## 12. 目标态分阶段验收清单

以下条目按能力启用阶段验收；进入某阶段或场景范围的能力必须满足配套蓝图中的规范、失败和自动化契约。

### 身份与账号

- [ ] 任意业务线创建用户后获得唯一且稳定的 Global User ID。
- [ ] 手机号或邮箱变化不影响 Global User ID。
- [ ] 一个用户可以绑定多个登录身份。
- [ ] 全局冻结和业务封禁互不混淆。
- [ ] 注销、合并、换绑等操作有完整审计。

### 认证与会话

- [ ] Web、App、小程序可以通过统一协议接入。
- [ ] 支持登录、刷新、退出、全局退出和 Token 吊销。
- [ ] 修改密码、冻结账号后，会话在约定时间内失效。
- [ ] 登录接口具备限流、验证码防滥用和枚举防护。
- [ ] 业务后端正确验证签发方、audience、签名和有效期。
- [ ] 所有客户端通过适用 OAuth/OIDC 安全 Profile，禁用弱流程和宽松回调。
- [ ] Refresh Token 重放会撤销整个 Token family。
- [ ] 全局退出覆盖身份平台、RP 本地会话和相关 Refresh Token，并有传播 SLA。
- [ ] 找回和认证器替换不会绕过 MFA 或静默提升保证等级。

### 资料与业务隔离

- [ ] 公共资料和业务资料有明确权威边界。
- [ ] 业务线只能读取和修改被授权的字段。
- [ ] 敏感资料在管理后台和日志中按规则脱敏。
- [ ] 用户资料变更事件可重试、幂等和对账。
- [ ] 手机号回收或邮箱重新分配不会使新持有人继承旧账号。
- [ ] 跨租户搜索、缓存、索引、事件和批量接口均通过负向隔离测试。

### 权限

- [ ] 权限至少支持角色、权限、业务/租户作用域和数据范围。
- [ ] 后端执行权限校验，不能只依赖前端控制。
- [ ] 权限变更、授予和使用可审计。
- [ ] 高风险操作支持实时授权判断。
- [ ] PDP 决策包含原因、义务、策略版本和有效期，PEP 在后端强制执行。
- [ ] 高风险权限撤回后不能命中过期缓存继续放行。
- [ ] Actor 与 Subject 可区分，代理和 Token Exchange 保留委托链。

### 事件、隐私与机器身份

- [ ] Webhook 可验签、防重放、轮换密钥并从断档主动补拉。
- [ ] Consent 撤回传播到 Grant、Token、订阅和相关下游处理。
- [ ] 删除流程覆盖阻断、重试、法律保留、备份策略和完成证明。
- [ ] 所有生产机器身份均有负责人、用途、环境、最小权限、到期和轮换。
- [ ] Client 或机器密钥失陷可紧急阻断新签发并确定存量影响范围。

### 运维与安全

- [ ] 定义并验证可用性、延迟、RTO、RPO 和容量目标。
- [ ] 关键监控、告警、值班和应急流程可用。
- [ ] 完成数据备份恢复和容灾演练。
- [ ] 管理员强制 MFA，高风险操作有审批。
- [ ] 不在日志中输出密码、验证码、完整 Token 和凭证密钥。
- [ ] 未审批控制面配置不能激活，根密钥和首个管理员具备可演练的恢复信任链。
- [ ] 协议负向测试、状态机属性测试、密钥轮换和租户隔离测试进入发布门禁。
- [ ] 风控覆盖注册、恢复、换绑、合并、授权、机器身份和管理员操作。

## 13. 必须尽早决策的问题

1. 平台如何判断两个账号属于同一个自然人，哪些情况禁止自动合并？
2. 是否同时支持个人用户、企业员工、商家、开发者和服务账号？
3. 手机号、邮箱、用户名的唯一性是全局、租户内还是应用内？
4. 哪些业务允许共享同一个 Subject，哪些业务必须使用应用级 Subject？
5. 用户能否在没有加入任何业务线时独立存在？
6. 用户资料中哪些字段由平台负责，哪些由业务负责？
7. 一个业务线封禁用户是否只影响该 Membership？
8. 注销是全局注销还是退出某个业务，两种入口如何区分？
9. 统一权限中心负责到什么粒度，哪些权限由业务自行管理？
10. 是否需要企业 SSO、租户、组织、部门和委派管理员？
11. 旧系统存在多少套账号、重复账号和不兼容密码算法？
12. 身份中心不可用时，已有登录态和敏感操作分别如何降级？
13. 业务运营地区、数据驻留、未成年人和实名要求是什么？
14. 平台可用性、最大冻结生效时间和灾备目标是什么？
15. 谁是用户数据、身份安全、权限策略和隐私治理的最终负责人？
16. 平台采用哪些 IAL/AAL/FAL 等级，各敏感操作需要何种等级和认证新鲜度？
17. 各类客户端适用哪个 OAuth/OIDC 安全 Profile，是否存在高价值 FAPI 场景？
18. Client、API Resource、机器身份和工作负载身份的负责人及下线机制是什么？
19. 平台、应用、设备、租户和上游 IdP 的退出边界及传播 SLA 是什么？
20. 手机号回收、企业邮箱重分配和域名所有权变化如何防止账号接管？
21. 控制面首个管理员、根密钥、租户所有权转移和灾备恢复信任链如何建立？
22. Consent、OAuth Grant、营销订阅和协议接受如何分别建模并向下游传播？
23. 哪些不变量必须由数据库约束、属性测试、持续对账和发布门禁分别保证？

## 14. 建议的能力所有权

| 领域 | 建议负责人 |
|---|---|
| UID、身份、认证、会话、Token | 身份平台团队 |
| 风控规则与安全事件 | 安全团队 + 身份平台团队 |
| 公共资料与字段标准 | 数据治理团队 + 身份平台团队 |
| 业务资料和业务成员状态 | 各业务线 |
| 权限模型与策略平台 | 权限平台团队或身份平台独立子域 |
| 企业组织源数据 | HR、企业目录或租户管理员 |
| 隐私同意、删除和保留 | 法务/隐私团队 + 数据治理团队 |
| 应用接入规范和 SDK | 身份平台团队 |
| 可用性、容量与灾备 | 身份平台团队 + SRE |
| OAuth/OIDC 安全 Profile 与一致性 | 身份平台团队 + 安全团队 |
| 机器身份与工作负载凭证 | 身份平台团队 + 平台工程/SRE |
| 控制面、配置发布和根信任 | 身份平台团队 + 安全团队 + SRE |
| 隐私请求跨系统编排 | 隐私团队 + 数据治理团队 + 各业务线 |

## 15. 参考标准方向

平台设计和实现可对齐以下标准或行业协议：

- OpenID Connect：统一身份认证和用户身份声明。
- OAuth 2.0：应用访问授权。
- RFC 9700 OAuth 2.0 Security Best Current Practice：OAuth 安全基线。
- RFC 7636 PKCE：授权码拦截防护，平台只允许 S256。
- RFC 9126 PAR、RFC 9101 JAR、RFC 9396 RAR：高安全授权请求。
- RFC 8705 OAuth mTLS、RFC 9449 DPoP：发送方约束 Token。
- RFC 8693 Token Exchange：受控委托和代理调用。
- RFC 9068 JWT Access Token Profile：JWT Access Token 互操作约束。
- OIDC RP-Initiated、Front-Channel、Back-Channel Logout：完整单点退出。
- FAPI 2.0 Security Profile：高价值 API 的安全 Profile。
- WebAuthn / FIDO2：Passkey 和抗钓鱼认证。
- SCIM 2.0：企业用户与组织目录同步。
- SAML 2.0：存量企业联合登录。
- JWT / JWK：令牌声明和签名密钥发布。
- NIST SP 800-63-4：身份核验、认证和联合保证等级参考。
- Shared Signals / CAEP / RISC：跨域持续安全状态与撤销信号。

标准协议解决的是互操作性，具体身份合并、租户隔离、业务状态、数据权限、隐私和运营流程仍需按本平台规则设计。
