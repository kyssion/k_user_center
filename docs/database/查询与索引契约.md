# 查询与索引契约

本文件登记当前基线中影响登录、授权、单次消费、队列、回放、密钥轮换和迁移的关键查询路径。索引只提供访问路径，不表达参数有效、引用存在、状态合法、租户一致或调用者有权；这些条件必须由代码完整校验。

| Query ID | 用途 | 主要过滤与排序 | 必需索引/约束 | 代码守卫 |
|---|---|---|---|---|
| `Q-ID-001` | 规范化 Identifier 查找 | `scope_type, scope_id, identifier_type, blind_index` | `ix_identifier_lookup` | 规范化版本、作用域权限、防枚举 |
| `Q-ID-002` | Identifier 历史绑定 | `identifier_id ORDER BY bound_at DESC` | `ix_identifier_bindings_identifier` | User 归属、当前状态、隔离期 |
| `Q-ID-003` | 当前 Subject | `user_id, client_id, current_subject_slot=1` | `uq_user_subject_current` | User/Client 状态与调用者 Client 作用域 |
| `Q-ID-004` | Token/事件 Subject 解析 | `client_id, subject_id` | `uq_user_subject_client_subject` | 接收方、退役窗口和披露规则 |
| `Q-ID-005` | Guest 到期扫描 | `user_type, guest_expires_at` | `ix_global_users_guest_expiry` | Profile、允许时钟偏差和处置策略 |
| `Q-AUTH-001` | Challenge 摘要单次消费 | `token_hash` | `uq_auth_challenge_token` | purpose、主体、Client/Tenant、状态、过期与尝试次数 |
| `Q-AUTH-002` | Challenge 过期扫描 | `state, expires_at` | `ix_auth_challenges_expiry` | 状态机和批次上限 |
| `Q-AUTH-003` | 用户认证器 | `user_id, state, authenticator_type` | `ix_authenticators_user` | User 生命周期、AAL、字段脱敏 |
| `Q-OAP-001` | 授权码兑换 | `code_hash` | `uq_authorization_codes_hash` | Client、Redirect、PKCE、Scope、状态和过期 |
| `Q-OAP-002` | Refresh Token 实例 | `token_hash` | `uq_refresh_token_hash` | Family、Client、User、设备、状态和安全水位 |
| `Q-OAP-003` | Family 轮换顺序 | `family_id ORDER BY sequence_no DESC` | `uq_refresh_token_sequence` | 当前指针、expected version 和重放策略 |
| `Q-OAP-004` | Access Token JTI 定位 | `jti`，必要时带签发时间范围 | `ix_access_token_jti` | 跨分区唯一性由生成与幂等代码保证 |
| `Q-OAP-005` | Actor Token 时间线 | `actor_type, actor_id ORDER BY issued_at DESC` | `ix_access_tokens_actor` | Actor 类型解析、Tenant 和调查权限 |
| `Q-OAP-006` | Consent Token 影响面 | `consent_id, consent_epoch ORDER BY issued_at DESC` | `ix_access_tokens_consent` | Consent Owner、撤回水位和时间窗 |
| `Q-OAP-007` | 决策签发 Token 追踪 | `authorization_decision_id ORDER BY issued_at DESC` | `ix_access_tokens_decision` | 决策可见性和调查权限 |
| `Q-OAP-008` | 策略版本 Token 影响面 | `policy_version_ids @> ARRAY[@policy_version_id]::uuid[]` | `ix_access_tokens_policy_versions_gin` | 策略 Owner、作用域和签发时间窗 |
| `Q-SESSION-001` | 用户有效会话 | `user_id, state, absolute_expires_at` | `ix_sessions_user_state` | User/Tenant、撤销水位和字段可见性 |
| `Q-SESSION-002` | Consent 影响会话 | `consent_id, consent_epoch` | `ix_sessions_consent` | Consent Owner 和撤回范围 |
| `Q-SESSION-003` | 策略版本会话影响面 | `policy_version_ids @> ARRAY[@policy_version_id]::uuid[]` | `ix_sessions_policy_versions_gin` | 策略 Owner、Tenant 和会话状态 |
| `Q-OPS-001` | Operation Worker 领取 | `state ORDER BY updated_at` | `ix_operations_queue` | 原子 CAS、租约、尝试上限和能力白名单 |
| `Q-OPS-002` | Outbox 发布领取 | `publish_state, next_attempt_at ORDER BY recorded_at` | `ix_outbox_publish_queue` | 原子领取、Schema 和发布重试策略 |
| `Q-OPS-003` | Inbox 消费状态 | `consumer_id, state, received_at` | `ix_inbox_state` | consumer 身份、事件类型和重放规则 |
| `Q-OPS-004` | 能力 Operation 时间线 | `capability_code, state ORDER BY created_at DESC` | `ix_operations_capability` | Actor/Subject/Tenant 和查看权限 |
| `Q-OPS-005` | 策略版本 Operation 影响面 | `policy_version_ids @> ARRAY[@policy_version_id]::uuid[]` | `ix_operations_policy_versions_gin` | 策略 Owner、能力和作用域 |
| `Q-OPS-006` | Outbox 时间归档扫描 | `recorded_at` | `ix_outbox_recorded_brin` | Legal Hold、发布完成和保留期 |
| `Q-OPS-007` | Inbox 时间归档扫描 | `received_at` | `ix_inbox_received_brin` | 消费完成、重放窗口和保留期 |
| `Q-EVENT-001` | Webhook Worker 领取 | `state, next_attempt_at ORDER BY created_at` | `ix_webhook_deliveries_queue` | 原子领取、订阅状态、SSRF 和签名策略 |
| `Q-EVENT-002` | Webhook 公开投递 ID | `delivery_id`，必要时带创建时间范围 | `ix_webhook_deliveries_delivery_id` | 跨分区唯一性由创建命令保证 |
| `Q-EVENT-003` | Event/Subscription 投递诊断 | `event_id, subscription_id ORDER BY created_at DESC` | `ix_webhook_deliveries_event_subscription` | 订阅 Owner、事件可见性和数据分类 |
| `Q-EVENT-004` | 投递尝试历史 | `delivery_id ORDER BY attempt_no, created_at` | `ix_webhook_attempts_delivery` | 尝试序号与幂等由 Worker 代码保证 |
| `Q-MSG-001` | 消息 Worker 领取 | `state, scheduled_at ORDER BY priority DESC, created_at` | `ix_message_requests_queue` | 原子领取、抑制、限速、模板和过期 |
| `Q-MSG-002` | 消息请求公开 ID | `request_id`，必要时带创建时间范围 | `ix_message_requests_request_id` | 跨分区唯一性由幂等命令保证 |
| `Q-MSG-003` | 消息尝试历史 | `request_id ORDER BY attempt_no, created_at` | `ix_message_attempts_request` | 尝试序号、供应商权限和目标脱敏 |
| `Q-RISK-001` | 风险信号公开 ID | `signal_id`，必要时带发生时间范围 | `ix_risk_signals_signal_id` | 跨分区唯一性由生产者与事件幂等保证 |
| `Q-RISK-002` | 主体风险信号时间线 | `subject_type, subject_id ORDER BY occurred_at DESC` | `ix_risk_signals_subject` | 主体类型、Tenant、保留期和调查权限 |
| `Q-RISK-003` | 风险信号影响评估 | `signal_id, assessment_id` | `ix_risk_assessment_signals_signal` | 信号与评估作用域、模型版本和可见性 |
| `Q-AUTHZ-001` | 用户有效角色 | `user_id, scope_type, scope_id, state, valid_until` | `ix_user_role_assignments_effective` | Tenant、职责分离、状态和有效期 |
| `Q-AUTHZ-002` | 资源关系元组 | `resource_type, resource_id, relation, state` | `ix_relationship_tuples_resource` | 资源 Owner、Tenant、主体类型和策略 |
| `Q-AUTHZ-003` | 授权决策公开 ID | `decision_id`，必要时带决策时间范围 | `ix_authorization_decisions_decision_id` | 跨分区唯一性由 PDP 与幂等代码保证 |
| `Q-AUTHZ-004` | 授权决策失效扫描 | `valid_until ORDER BY decided_at DESC` | `ix_authorization_decisions_validity` | 策略版本、撤销水位和安全时间窗 |
| `Q-AUTHZ-005` | 策略版本决策影响面 | `policy_version_ids @> ARRAY[@policy_version_id]::uuid[]` | `ix_authorization_decisions_policy_versions_gin` | 策略 Owner、资源作用域和调查权限 |
| `Q-AUTHZ-006` | 角色变更授予影响面 | `role_id, state, valid_until` | `ix_user_role_assignments_role`、`ix_group_role_assignments_role`、`ix_machine_role_assignments_role` | 角色 Owner、Scope、受授主体和职责分离 |
| `Q-TENANT-001` | 用户 Membership | `user_id, state, tenant_id` | `ix_memberships_user` | Business Line/Tenant/Organization 链与数据范围 |
| `Q-CTRL-001` | 配置版本所属发布单 | `configuration_version_id, release_id` | `ix_configuration_release_items_version` | 配置 Owner、环境、审批和发布状态 |
| `Q-KEY-001` | Identifier Key 影响面 | `key_id` | `ix_identifiers_key` | Key Owner、用途、Tenant 和轮换权限 |
| `Q-KEY-002` | 认证材料 Key 影响面 | `key_id` | `ix_credential_materials_key` | 敏感角色、材料状态和轮换策略 |
| `Q-KEY-003` | 隐私导出物 Key 影响面 | `key_id` | `ix_data_export_artifacts_key` | 隐私请求、保留期和销毁审批 |
| `Q-KEY-004` | JWKS 发布 Key 影响面 | `key_id` | `ix_jwks_release_keys_key` | Owner、用途和验证重叠窗口 |
| `Q-KEY-005` | 证书 Key 影响面 | `key_id` | `ix_certificates_key` | 证书 Owner、用途和有效期 |
| `Q-KEY-006` | 机器凭证 Key/证书影响面 | `key_id` 或 `certificate_id` | `ix_machine_credentials_key`、`ix_machine_credentials_certificate` | 机器 Owner、环境和凭证状态 |
| `Q-KEY-007` | Webhook 签名 Key 影响面 | `key_id` | `ix_webhook_signing_keys_key` | 订阅 Owner、状态和接收方轮换协议 |
| `Q-FED-001` | 目录同步 Worker | `state ORDER BY updated_at` | `ix_directory_sync_batches_queue` | Connector/Tenant、租约、游标和失败隔离 |
| `Q-MIG-001` | 迁移项 Worker | `state, next_attempt_at ORDER BY updated_at` | `ix_migration_items_queue` | Legacy System、批次、幂等和切换阶段 |

## 代码约束

- 任何参数、JSON Schema、协议字段和业务枚举先由代码校验；数据库错误不是常规输入校验流程。
- 不允许为了复用索引而省略 Tenant、Business Line、Owner、状态、过期时间、数据范围或软删除条件。
- 公共 ID、Token 摘要和唯一占用查询使用等值条件；Range 分区公开 ID 查询应尽量携带时间范围，否则会扫描多个分区索引。
- Worker 领取必须使用原子条件 UPDATE/CAS；SELECT 命中索引不表示已经获得任务所有权。
- 新增或改变高频查询时先更新本文件，再评审索引；禁止机械地为每个 `*_id` 创建单列索引。
- GIN 数组查询只用于反向影响分析，不证明数组中的逻辑引用有效；写入时仍需逐项校验策略版本、Owner、Scope 和状态。
- PostgreSQL 实跑阶段使用代表性数据量验证执行计划、分区裁剪、排序、GIN 选择性和写放大。
