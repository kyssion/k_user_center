# 逻辑关系与非数据库校验清单

> 本文件由 Migration 的 Column Comment 生成，只登记数据库可识别的逻辑引用。在不创建 Foreign Key 的前提下，目标存在性、作用域、生命周期、删除行为和多态解析属于非数据库职责，具体实现以后续代码实施文档为准。

- 逻辑引用字段：215
- 可执行 SQL 孤儿检查：192
- 多态或代码解析引用：23

| 来源字段 | 类型 | 目标 | 非数据库校验提示 | Comment |
|---|---|---|---|---|
| `iam.access_token_records.actor_id` | `POLYMORPHIC` | 由类型字段或代码注册表解析 | 校验类型白名单、目标存在、租户/业务线作用域和生命周期；负向测试未知类型与跨租户引用。 | 可空；按 actor_type 逻辑引用自然人、机器主体或 Client。 |
| `iam.access_token_records.authorization_decision_id` | `DIRECT` | `iam.authorization_decisions.decision_id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.authorization_decisions.decision_id，记录签发依据的 PDP 全局决策。 |
| `iam.access_token_records.client_id` | `DIRECT` | `iam.oauth_clients.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.oauth_clients.id。 |
| `iam.access_token_records.consent_id` | `DIRECT` | `iam.consents.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；以 Consent 为处理依据时逻辑引用 iam.consents.id。 |
| `iam.access_token_records.delegation_id` | `DIRECT` | `iam.delegations.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.delegations.id。 |
| `iam.access_token_records.grant_id` | `DIRECT` | `iam.authorization_grants.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.authorization_grants.id。 |
| `iam.access_token_records.policy_version_ids` | `ARRAY` | `iam.policy_versions.id` | 逐项校验目标存在、作用域和生命周期；写入与删除前运行集合校验。 | 签发决策使用的 iam.policy_versions.id 逻辑引用数组。 |
| `iam.access_token_records.session_id` | `DIRECT` | `iam.sessions.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.sessions.id。 |
| `iam.access_token_records.user_id` | `DIRECT` | `iam.global_users.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；自然人 Token 逻辑引用 iam.global_users.id。 |
| `iam.agreement_acceptances.agreement_version_id` | `DIRECT` | `iam.agreement_versions.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.agreement_versions.id。 |
| `iam.agreement_acceptances.user_id` | `DIRECT` | `iam.global_users.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.global_users.id。 |
| `iam.api_resources.business_line_id` | `DIRECT` | `iam.business_lines.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.business_lines.id。 |
| `iam.applications.business_line_id` | `DIRECT` | `iam.business_lines.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.business_lines.id。 |
| `iam.approval_actions.approval_case_id` | `DIRECT` | `iam.approval_cases.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.approval_cases.id。 |
| `iam.approval_cases.policy_version_id` | `POLYMORPHIC` | 由类型字段或代码注册表解析 | 校验类型白名单、目标存在、租户/业务线作用域和生命周期；负向测试未知类型与跨租户引用。 | 可空；逻辑引用审批策略版本。 |
| `iam.approval_cases.tenant_id` | `DIRECT` | `iam.tenants.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.tenants.id。 |
| `iam.audit_events.actor_id` | `POLYMORPHIC` | 由类型字段或代码注册表解析 | 校验类型白名单、目标存在、租户/业务线作用域和生命周期；负向测试未知类型与跨租户引用。 | 可空；按 actor_type 逻辑引用主体表，数据库不创建外键。 |
| `iam.audit_events.approval_case_id` | `DIRECT` | `iam.approval_cases.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.approval_cases.id。 |
| `iam.audit_events.subject_id` | `POLYMORPHIC` | 由类型字段或代码注册表解析 | 校验类型白名单、目标存在、租户/业务线作用域和生命周期；负向测试未知类型与跨租户引用。 | 可空；按 subject_type 逻辑引用主体表。 |
| `iam.audit_events.tenant_id` | `DIRECT` | `iam.tenants.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.tenants.id。 |
| `iam.auth_challenges.client_id` | `DIRECT` | `iam.oauth_clients.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.oauth_clients.id。 |
| `iam.authentication_attempts.client_id` | `DIRECT` | `iam.oauth_clients.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.oauth_clients.id。 |
| `iam.authentication_attempts.device_id` | `DIRECT` | `iam.devices.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.devices.id。 |
| `iam.authentication_attempts.tenant_id` | `DIRECT` | `iam.tenants.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.tenants.id。 |
| `iam.authentication_attempts.user_id` | `DIRECT` | `iam.global_users.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；已识别时逻辑引用 iam.global_users.id。 |
| `iam.authentication_contexts.login_transaction_id` | `DIRECT` | `iam.login_transactions.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.login_transactions.id。 |
| `iam.authentication_contexts.user_id` | `DIRECT` | `iam.global_users.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.global_users.id。 |
| `iam.authenticators.replaced_by_id` | `DIRECT` | `iam.authenticators.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.authenticators.id。 |
| `iam.authenticators.user_id` | `DIRECT` | `iam.global_users.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.global_users.id；写入前校验用户生命周期。 |
| `iam.authorization_codes.client_id` | `DIRECT` | `iam.oauth_clients.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.oauth_clients.id。 |
| `iam.authorization_codes.login_transaction_id` | `DIRECT` | `iam.login_transactions.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.login_transactions.id。 |
| `iam.authorization_codes.user_id` | `DIRECT` | `iam.global_users.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.global_users.id。 |
| `iam.authorization_decisions.actor_id` | `POLYMORPHIC` | 由类型字段或代码注册表解析 | 校验类型白名单、目标存在、租户/业务线作用域和生命周期；负向测试未知类型与跨租户引用。 | 可空；按 actor_type 逻辑引用自然人、机器主体或 Client。 |
| `iam.authorization_decisions.consent_id` | `DIRECT` | `iam.consents.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；以 Consent 为依据时逻辑引用 iam.consents.id。 |
| `iam.authorization_decisions.delegation_id` | `DIRECT` | `iam.delegations.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.delegations.id。 |
| `iam.authorization_decisions.policy_version_ids` | `ARRAY` | `iam.policy_versions.id` | 逐项校验目标存在、作用域和生命周期；写入与删除前运行集合校验。 | 参与决策的 iam.policy_versions.id 逻辑引用数组。 |
| `iam.authorization_decisions.tenant_id` | `DIRECT` | `iam.tenants.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.tenants.id。 |
| `iam.authorization_grants.client_id` | `DIRECT` | `iam.oauth_clients.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.oauth_clients.id。 |
| `iam.authorization_grants.consent_id` | `DIRECT` | `iam.consents.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.consents.id。 |
| `iam.authorization_grants.resource_id` | `DIRECT` | `iam.api_resources.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.api_resources.id。 |
| `iam.authorization_grants.tenant_id` | `DIRECT` | `iam.tenants.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.tenants.id。 |
| `iam.authorization_grants.user_id` | `DIRECT` | `iam.global_users.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.global_users.id。 |
| `iam.business_lines.active_configuration_id` | `DIRECT` | `iam.configuration_versions.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.configuration_versions.id。 |
| `iam.business_lines.owner_id` | `POLYMORPHIC` | 由类型字段或代码注册表解析 | 校验类型白名单、目标存在、租户/业务线作用域和生命周期；负向测试未知类型与跨租户引用。 | 按 owner_type 逻辑引用责任主体，数据库不创建外键。 |
| `iam.certificates.key_id` | `DIRECT` | `iam.cryptographic_keys.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.cryptographic_keys.id。 |
| `iam.configuration_release_items.configuration_version_id` | `DIRECT` | `iam.configuration_versions.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.configuration_versions.id。 |
| `iam.configuration_release_items.release_id` | `DIRECT` | `iam.configuration_releases.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.configuration_releases.id。 |
| `iam.configuration_releases.approval_case_id` | `DIRECT` | `iam.approval_cases.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.approval_cases.id。 |
| `iam.configuration_versions.approved_by_case_id` | `DIRECT` | `iam.approval_cases.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.approval_cases.id。 |
| `iam.configuration_versions.scope_id` | `POLYMORPHIC` | 由类型字段或代码注册表解析 | 校验类型白名单、目标存在、租户/业务线作用域和生命周期；负向测试未知类型与跨租户引用。 | 可空；按 scope_type 逻辑引用作用域对象，平台级配置为空。 |
| `iam.consent_aggregates.current_consent_id` | `DIRECT` | `iam.consents.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用当前 iam.consents.id。 |
| `iam.consents.aggregate_id` | `DIRECT` | `iam.consent_aggregates.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.consent_aggregates.id。 |
| `iam.consents.policy_version_id` | `DIRECT` | `iam.policy_versions.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.policy_versions.id。 |
| `iam.contact_reachability.identifier_id` | `DIRECT` | `iam.identifiers.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.identifiers.id。 |
| `iam.credential_materials.authenticator_id` | `DIRECT` | `iam.authenticators.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.authenticators.id。 |
| `iam.credential_materials.key_id` | `DIRECT` | `iam.cryptographic_keys.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.cryptographic_keys.id 或外部 KMS Key。 |
| `iam.cryptographic_keys.owner_id` | `POLYMORPHIC` | 由类型字段或代码注册表解析 | 校验类型白名单、目标存在、租户/业务线作用域和生命周期；负向测试未知类型与跨租户引用。 | 可空；平台密钥为空，其他按类型逻辑引用。 |
| `iam.data_export_artifacts.key_id` | `DIRECT` | `iam.cryptographic_keys.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.cryptographic_keys.id 或 KMS Key 元数据。 |
| `iam.data_export_artifacts.privacy_request_id` | `DIRECT` | `iam.privacy_requests.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.privacy_requests.id。 |
| `iam.data_scope_definitions.owner_id` | `POLYMORPHIC` | 由类型字段或代码注册表解析 | 校验类型白名单、目标存在、租户/业务线作用域和生命周期；负向测试未知类型与跨租户引用。 | 可空；按 owner_type 逻辑引用所有者对象，平台定义为空。 |
| `iam.delegations.actor_user_id` | `DIRECT` | `iam.global_users.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 代理人，逻辑引用 iam.global_users.id。 |
| `iam.delegations.subject_user_id` | `DIRECT` | `iam.global_users.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 被代表人，逻辑引用 iam.global_users.id。 |
| `iam.delegations.tenant_id` | `DIRECT` | `iam.tenants.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.tenants.id。 |
| `iam.deletion_proofs.privacy_request_id` | `DIRECT` | `iam.privacy_requests.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.privacy_requests.id。 |
| `iam.devices.user_id` | `DIRECT` | `iam.global_users.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.global_users.id。 |
| `iam.directory_connectors.active_configuration_id` | `DIRECT` | `iam.configuration_versions.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.configuration_versions.id。 |
| `iam.directory_connectors.provider_id` | `DIRECT` | `iam.identity_providers.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.identity_providers.id。 |
| `iam.directory_connectors.tenant_id` | `DIRECT` | `iam.tenants.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.tenants.id。 |
| `iam.directory_object_mappings.connector_id` | `DIRECT` | `iam.directory_connectors.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.directory_connectors.id。 |
| `iam.directory_object_mappings.platform_object_id` | `POLYMORPHIC` | 由类型字段或代码注册表解析 | 校验类型白名单、目标存在、租户/业务线作用域和生命周期；负向测试未知类型与跨租户引用。 | 按类型逻辑引用平台对象。 |
| `iam.directory_sync_batches.connector_id` | `DIRECT` | `iam.directory_connectors.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.directory_connectors.id。 |
| `iam.directory_sync_batches.operation_id` | `DIRECT` | `iam.operations.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.operations.id。 |
| `iam.directory_sync_cursors.connector_id` | `DIRECT` | `iam.directory_connectors.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.directory_connectors.id。 |
| `iam.event_replay_requests.approval_case_id` | `DIRECT` | `iam.approval_cases.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.approval_cases.id。 |
| `iam.event_replay_requests.operation_id` | `DIRECT` | `iam.operations.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.operations.id。 |
| `iam.event_schema_versions.approval_case_id` | `DIRECT` | `iam.approval_cases.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；发布审批逻辑引用 iam.approval_cases.id。 |
| `iam.group_members.group_id` | `DIRECT` | `iam.groups.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.groups.id。 |
| `iam.group_members.membership_id` | `DIRECT` | `iam.memberships.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.memberships.id。 |
| `iam.group_members.user_id` | `DIRECT` | `iam.global_users.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.global_users.id。 |
| `iam.group_role_assignments.group_id` | `DIRECT` | `iam.groups.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.groups.id。 |
| `iam.group_role_assignments.role_id` | `DIRECT` | `iam.roles.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.roles.id。 |
| `iam.groups.organization_id` | `DIRECT` | `iam.organizations.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.organizations.id。 |
| `iam.groups.source_connector_id` | `DIRECT` | `iam.directory_connectors.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.directory_connectors.id。 |
| `iam.groups.tenant_id` | `DIRECT` | `iam.tenants.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.tenants.id。 |
| `iam.idempotency_records.operation_id` | `DIRECT` | `iam.operations.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.operations.id，数据库不创建外键，由 OPS 代码校验。 |
| `iam.identifier_bindings.identifier_id` | `DIRECT` | `iam.identifiers.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.identifiers.id。 |
| `iam.identifier_bindings.user_id` | `DIRECT` | `iam.global_users.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.global_users.id。 |
| `iam.identifier_claims.identifier_id` | `DIRECT` | `iam.identifiers.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.identifiers.id；数据库不创建外键。 |
| `iam.identifier_claims.owner_user_id` | `DIRECT` | `iam.global_users.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.global_users.id；代码校验归属与状态。 |
| `iam.identifiers.key_id` | `DIRECT` | `iam.cryptographic_keys.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.cryptographic_keys.id 或外部 KMS Key 元数据。 |
| `iam.identifiers.scope_id` | `POLYMORPHIC` | 由类型字段或代码注册表解析 | 校验类型白名单、目标存在、租户/业务线作用域和生命周期；负向测试未知类型与跨租户引用。 | 可空；按 scope_type 逻辑引用作用域对象，全局作用域为空。 |
| `iam.identity_assurance_assertions.provider_id` | `POLYMORPHIC` | 由类型字段或代码注册表解析 | 校验类型白名单、目标存在、租户/业务线作用域和生命周期；负向测试未知类型与跨租户引用。 | 可空；逻辑引用身份提供者、应用或外部服务登记。 |
| `iam.identity_assurance_assertions.user_id` | `DIRECT` | `iam.global_users.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.global_users.id。 |
| `iam.identity_providers.active_configuration_id` | `DIRECT` | `iam.configuration_versions.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.configuration_versions.id。 |
| `iam.identity_providers.tenant_id` | `DIRECT` | `iam.tenants.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.tenants.id，平台共享身份源为空。 |
| `iam.invitations.accepted_by_user_id` | `DIRECT` | `iam.global_users.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；接受者逻辑引用 iam.global_users.id。 |
| `iam.invitations.inviter_machine_id` | `DIRECT` | `iam.machine_principals.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.machine_principals.id。 |
| `iam.invitations.inviter_user_id` | `DIRECT` | `iam.global_users.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.global_users.id。 |
| `iam.invitations.scope_id` | `POLYMORPHIC` | 由类型字段或代码注册表解析 | 校验类型白名单、目标存在、租户/业务线作用域和生命周期；负向测试未知类型与跨租户引用。 | 按 scope_type 逻辑引用作用域对象。 |
| `iam.jwks_release_keys.jwks_release_id` | `DIRECT` | `iam.jwks_releases.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.jwks_releases.id。 |
| `iam.jwks_release_keys.key_id` | `DIRECT` | `iam.cryptographic_keys.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.cryptographic_keys.id。 |
| `iam.legacy_id_mappings.platform_id` | `POLYMORPHIC` | 由类型字段或代码注册表解析 | 校验类型白名单、目标存在、租户/业务线作用域和生命周期；负向测试未知类型与跨租户引用。 | 按 platform_type 逻辑引用平台对象。 |
| `iam.legacy_id_mappings.system_id` | `DIRECT` | `iam.legacy_systems.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.legacy_systems.id。 |
| `iam.legacy_systems.active_configuration_id` | `DIRECT` | `iam.configuration_versions.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.configuration_versions.id。 |
| `iam.login_transaction_steps.login_transaction_id` | `DIRECT` | `iam.login_transactions.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.login_transactions.id。 |
| `iam.login_transactions.client_id` | `DIRECT` | `iam.oauth_clients.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.oauth_clients.id。 |
| `iam.login_transactions.tenant_id` | `DIRECT` | `iam.tenants.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.tenants.id。 |
| `iam.login_transactions.user_id` | `DIRECT` | `iam.global_users.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；身份识别完成后逻辑引用 iam.global_users.id。 |
| `iam.machine_credentials.certificate_id` | `DIRECT` | `iam.certificates.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.certificates.id。 |
| `iam.machine_credentials.client_id` | `DIRECT` | `iam.oauth_clients.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.oauth_clients.id。 |
| `iam.machine_credentials.key_id` | `DIRECT` | `iam.cryptographic_keys.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.cryptographic_keys.id。 |
| `iam.machine_credentials.machine_principal_id` | `DIRECT` | `iam.machine_principals.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.machine_principals.id。 |
| `iam.machine_credentials.replaces_credential_id` | `DIRECT` | `iam.machine_credentials.id` | 写入前校验旧凭证存在且属于同一主体/Client，并满足轮换状态；删除时保留替代链或执行受控墓碑。 | 可空；逻辑引用 iam.machine_credentials.id；记录本凭证唯一替代的旧凭证，数据库不判断旧凭证状态和轮换窗口。 |
| `iam.machine_principals.tenant_id` | `DIRECT` | `iam.tenants.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.tenants.id。 |
| `iam.machine_role_assignments.machine_principal_id` | `DIRECT` | `iam.machine_principals.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.machine_principals.id。 |
| `iam.machine_role_assignments.role_id` | `DIRECT` | `iam.roles.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.roles.id。 |
| `iam.memberships.business_line_id` | `DIRECT` | `iam.business_lines.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.business_lines.id；便于隔离查询。 |
| `iam.memberships.organization_id` | `DIRECT` | `iam.organizations.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.organizations.id。 |
| `iam.memberships.scope_id` | `POLYMORPHIC` | 由类型字段或代码注册表解析 | 校验类型白名单、目标存在、租户/业务线作用域和生命周期；负向测试未知类型与跨租户引用。 | 按 scope_type 逻辑引用业务线、租户或组织。 |
| `iam.memberships.tenant_id` | `DIRECT` | `iam.tenants.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.tenants.id。 |
| `iam.memberships.user_id` | `DIRECT` | `iam.global_users.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.global_users.id。 |
| `iam.message_delivery_attempts.provider_id` | `DIRECT` | `iam.message_providers.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.message_providers.id。 |
| `iam.message_delivery_attempts.request_id` | `DIRECT` | `iam.message_requests.request_id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.message_requests.request_id。 |
| `iam.message_providers.active_configuration_id` | `DIRECT` | `iam.configuration_versions.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.configuration_versions.id。 |
| `iam.message_requests.identifier_id` | `DIRECT` | `iam.identifiers.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.identifiers.id。 |
| `iam.message_requests.template_version_id` | `DIRECT` | `iam.message_template_versions.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.message_template_versions.id。 |
| `iam.message_template_versions.approval_case_id` | `DIRECT` | `iam.approval_cases.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.approval_cases.id。 |
| `iam.migration_batches.operation_id` | `DIRECT` | `iam.operations.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.operations.id。 |
| `iam.migration_batches.system_id` | `DIRECT` | `iam.legacy_systems.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.legacy_systems.id。 |
| `iam.migration_change_logs.system_id` | `DIRECT` | `iam.legacy_systems.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.legacy_systems.id。 |
| `iam.migration_items.batch_id` | `DIRECT` | `iam.migration_batches.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.migration_batches.id。 |
| `iam.oauth_clients.active_configuration_id` | `DIRECT` | `iam.configuration_versions.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.configuration_versions.id。 |
| `iam.oauth_clients.application_id` | `DIRECT` | `iam.applications.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.applications.id。 |
| `iam.oauth_scopes.resource_id` | `DIRECT` | `iam.api_resources.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.api_resources.id，跨资源标准 Scope 可为空。 |
| `iam.operation_steps.operation_id` | `DIRECT` | `iam.operations.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.operations.id；数据库不创建外键，由 OPS 仓储校验。 |
| `iam.operations.actor_id` | `POLYMORPHIC` | 由类型字段或代码注册表解析 | 校验类型白名单、目标存在、租户/业务线作用域和生命周期；负向测试未知类型与跨租户引用。 | 可空；按 actor_type 逻辑引用主体表，数据库不创建外键。 |
| `iam.operations.policy_version_ids` | `ARRAY` | `iam.policy_versions.id` | 逐项校验目标存在、作用域和生命周期；写入与删除前运行集合校验。 | Operation 创建时适用的 iam.policy_versions.id 逻辑引用数组。 |
| `iam.operations.subject_id` | `POLYMORPHIC` | 由类型字段或代码注册表解析 | 校验类型白名单、目标存在、租户/业务线作用域和生命周期；负向测试未知类型与跨租户引用。 | 可空；按 subject_type 逻辑引用目标表，由代码校验作用域。 |
| `iam.operations.tenant_id` | `DIRECT` | `iam.tenants.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.tenants.id，平台级操作为空。 |
| `iam.organizations.parent_organization_id` | `DIRECT` | `iam.organizations.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.organizations.id，根节点为空。 |
| `iam.organizations.tenant_id` | `DIRECT` | `iam.tenants.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.tenants.id。 |
| `iam.outbox_events.actor_id` | `POLYMORPHIC` | 由类型字段或代码注册表解析 | 校验类型白名单、目标存在、租户/业务线作用域和生命周期；负向测试未知类型与跨租户引用。 | 可空；按 actor_type 与 actor_id_type 逻辑引用自然人、机器主体或 Client；对外事件由代码执行 pairwise 改写或移除。 |
| `iam.outbox_events.business_line_id` | `DIRECT` | `iam.business_lines.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.business_lines.id，用于业务线隔离和路由。 |
| `iam.outbox_events.producer_id` | `POLYMORPHIC` | 由类型字段或代码注册表解析 | 校验类型白名单、目标存在、租户/业务线作用域和生命周期；负向测试未知类型与跨租户引用。 | 按 producer_type 逻辑引用机器主体、Client 或系统主体；由代码解析和鉴权。 |
| `iam.outbox_events.tenant_id` | `DIRECT` | `iam.tenants.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.tenants.id，平台级事件为空。 |
| `iam.password_history.authenticator_id` | `DIRECT` | `iam.authenticators.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.authenticators.id。 |
| `iam.password_history.user_id` | `DIRECT` | `iam.global_users.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.global_users.id。 |
| `iam.policy_bindings.policy_version_id` | `DIRECT` | `iam.policy_versions.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.policy_versions.id。 |
| `iam.privacy_requests.identity_verification_id` | `DIRECT` | `iam.identity_assurance_assertions.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.identity_assurance_assertions.id。 |
| `iam.privacy_requests.operation_id` | `DIRECT` | `iam.operations.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.operations.id，用于跨系统编排。 |
| `iam.privacy_requests.user_id` | `DIRECT` | `iam.global_users.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.global_users.id。 |
| `iam.profile_documents.scope_id` | `POLYMORPHIC` | 由类型字段或代码注册表解析 | 校验类型白名单、目标存在、租户/业务线作用域和生命周期；负向测试未知类型与跨租户引用。 | 可空；按 scope_type 逻辑引用作用域对象。 |
| `iam.profile_documents.user_id` | `DIRECT` | `iam.global_users.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.global_users.id。 |
| `iam.recovery_code_batches.user_id` | `DIRECT` | `iam.global_users.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.global_users.id。 |
| `iam.recovery_codes.batch_id` | `DIRECT` | `iam.recovery_code_batches.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.recovery_code_batches.id。 |
| `iam.refresh_token_instances.family_id` | `DIRECT` | `iam.token_families.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.token_families.id。 |
| `iam.refresh_token_instances.replaced_by_id` | `DIRECT` | `iam.refresh_token_instances.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用下一 iam.refresh_token_instances.id。 |
| `iam.relationship_tuples.tenant_id` | `DIRECT` | `iam.tenants.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.tenants.id。 |
| `iam.resource_quotas.configuration_version_id` | `DIRECT` | `iam.configuration_versions.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.configuration_versions.id。 |
| `iam.resource_quotas.scope_id` | `POLYMORPHIC` | 由类型字段或代码注册表解析 | 校验类型白名单、目标存在、租户/业务线作用域和生命周期；负向测试未知类型与跨租户引用。 | 按 scope_type 逻辑引用作用域对象。 |
| `iam.risk_assessment_signals.assessment_id` | `DIRECT` | `iam.risk_assessments.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.risk_assessments.id。 |
| `iam.risk_assessment_signals.signal_id` | `DIRECT` | `iam.risk_signals.signal_id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.risk_signals.signal_id。 |
| `iam.risk_assessments.model_version_id` | `DIRECT` | `iam.configuration_versions.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 RISK_MODEL 类型 iam.configuration_versions.id。 |
| `iam.risk_assessments.policy_version_id` | `POLYMORPHIC` | 由类型字段或代码注册表解析 | 校验类型白名单、目标存在、租户/业务线作用域和生命周期；负向测试未知类型与跨租户引用。 | 可空；逻辑引用 iam.configuration_versions.id 或 iam.policy_versions.id，类型由代码登记。 |
| `iam.risk_assessments.tenant_id` | `DIRECT` | `iam.tenants.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.tenants.id。 |
| `iam.risk_cases.tenant_id` | `DIRECT` | `iam.tenants.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.tenants.id。 |
| `iam.risk_signals.tenant_id` | `DIRECT` | `iam.tenants.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.tenants.id。 |
| `iam.role_permissions.permission_id` | `DIRECT` | `iam.permissions.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.permissions.id。 |
| `iam.role_permissions.role_id` | `DIRECT` | `iam.roles.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.roles.id。 |
| `iam.roles.owner_id` | `POLYMORPHIC` | 由类型字段或代码注册表解析 | 校验类型白名单、目标存在、租户/业务线作用域和生命周期；负向测试未知类型与跨租户引用。 | 可空；平台内置角色为空，其他按 owner_type 逻辑引用。 |
| `iam.security_exceptions.approval_case_id` | `DIRECT` | `iam.approval_cases.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.approval_cases.id。 |
| `iam.security_signals.client_id` | `DIRECT` | `iam.oauth_clients.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.oauth_clients.id。 |
| `iam.security_signals.session_id` | `DIRECT` | `iam.sessions.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.sessions.id。 |
| `iam.session_participants.rp_client_id` | `DIRECT` | `iam.oauth_clients.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.oauth_clients.id。 |
| `iam.session_participants.session_id` | `DIRECT` | `iam.sessions.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.sessions.id。 |
| `iam.sessions.authentication_context_id` | `DIRECT` | `iam.authentication_contexts.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.authentication_contexts.id。 |
| `iam.sessions.client_id` | `DIRECT` | `iam.oauth_clients.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.oauth_clients.id。 |
| `iam.sessions.consent_id` | `DIRECT` | `iam.consents.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；以 Consent 为处理依据时逻辑引用 iam.consents.id。 |
| `iam.sessions.device_id` | `DIRECT` | `iam.devices.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.devices.id。 |
| `iam.sessions.policy_version_ids` | `ARRAY` | `iam.policy_versions.id` | 逐项校验目标存在、作用域和生命周期；写入与删除前运行集合校验。 | 创建会话时适用的 iam.policy_versions.id 逻辑引用数组。 |
| `iam.sessions.tenant_id` | `DIRECT` | `iam.tenants.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.tenants.id。 |
| `iam.sessions.user_id` | `DIRECT` | `iam.global_users.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.global_users.id。 |
| `iam.tenant_domains.tenant_id` | `DIRECT` | `iam.tenants.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.tenants.id。 |
| `iam.tenants.active_configuration_id` | `DIRECT` | `iam.configuration_versions.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.configuration_versions.id。 |
| `iam.tenants.business_line_id` | `DIRECT` | `iam.business_lines.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.business_lines.id。 |
| `iam.token_families.client_id` | `DIRECT` | `iam.oauth_clients.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.oauth_clients.id。 |
| `iam.token_families.current_instance_id` | `DIRECT` | `iam.refresh_token_instances.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.refresh_token_instances.id。 |
| `iam.token_families.device_id` | `DIRECT` | `iam.devices.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.devices.id。 |
| `iam.token_families.grant_id` | `DIRECT` | `iam.authorization_grants.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.authorization_grants.id。 |
| `iam.token_families.user_id` | `DIRECT` | `iam.global_users.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.global_users.id。 |
| `iam.usage_records.application_id` | `DIRECT` | `iam.applications.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.applications.id。 |
| `iam.usage_records.business_line_id` | `DIRECT` | `iam.business_lines.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.business_lines.id。 |
| `iam.usage_records.tenant_id` | `DIRECT` | `iam.tenants.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.tenants.id。 |
| `iam.user_aliases.canonical_user_id` | `DIRECT` | `iam.global_users.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.global_users.id；代码防止循环和跨租户错误。 |
| `iam.user_aliases.source_system_id` | `DIRECT` | `iam.legacy_systems.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.legacy_systems.id。 |
| `iam.user_identities.identifier_id` | `DIRECT` | `iam.identifiers.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；本地身份逻辑引用 iam.identifiers.id。 |
| `iam.user_identities.provider_id` | `DIRECT` | `iam.identity_providers.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；联合身份逻辑引用 iam.identity_providers.id。 |
| `iam.user_identities.user_id` | `DIRECT` | `iam.global_users.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.global_users.id。 |
| `iam.user_profiles.primary_contact_identifier_id` | `DIRECT` | `iam.identifiers.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.identifiers.id；代码校验其属于当前用户、已验证且当前有效绑定。 |
| `iam.user_profiles.user_id` | `DIRECT` | `iam.global_users.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.global_users.id，每个用户最多一条稳定 Profile。 |
| `iam.user_role_assignments.role_id` | `DIRECT` | `iam.roles.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.roles.id。 |
| `iam.user_role_assignments.user_id` | `DIRECT` | `iam.global_users.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.global_users.id。 |
| `iam.user_subjects.client_id` | `DIRECT` | `iam.oauth_clients.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.oauth_clients.id；写入前校验 Client 状态和业务线。 |
| `iam.user_subjects.user_id` | `DIRECT` | `iam.global_users.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.global_users.id；数据库不创建外键。 |
| `iam.webhook_deliveries.event_id` | `DIRECT` | `iam.outbox_events.event_id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.outbox_events.event_id 或事件总线事件 ID。 |
| `iam.webhook_deliveries.subscription_id` | `DIRECT` | `iam.webhook_subscriptions.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.webhook_subscriptions.id。 |
| `iam.webhook_delivery_attempts.delivery_id` | `DIRECT` | `iam.webhook_deliveries.delivery_id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.webhook_deliveries.delivery_id。 |
| `iam.webhook_delivery_attempts.signing_key_id` | `DIRECT` | `iam.webhook_signing_keys.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.webhook_signing_keys.id。 |
| `iam.webhook_signing_keys.key_id` | `DIRECT` | `iam.cryptographic_keys.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.cryptographic_keys.id。 |
| `iam.webhook_signing_keys.subscription_id` | `DIRECT` | `iam.webhook_subscriptions.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.webhook_subscriptions.id。 |
| `iam.webhook_subscriptions.active_configuration_id` | `DIRECT` | `iam.configuration_versions.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.configuration_versions.id。 |
| `iam.webhook_subscriptions.client_id` | `DIRECT` | `iam.oauth_clients.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.oauth_clients.id。 |
| `iam.webhook_subscriptions.tenant_id` | `DIRECT` | `iam.tenants.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；逻辑引用 iam.tenants.id。 |
| `iam.workload_attestations.machine_principal_id` | `DIRECT` | `iam.machine_principals.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 可空；验证识别后逻辑引用 iam.machine_principals.id。 |
| `iam.workload_attestations.trust_bundle_version_id` | `DIRECT` | `iam.workload_trust_bundle_versions.id` | 写入前校验目标存在、租户/业务线作用域和生命周期；删除或匿名化执行反向引用检查。 | 逻辑引用 iam.workload_trust_bundle_versions.id。 |
